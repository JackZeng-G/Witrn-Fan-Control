# WITRN 风扇蓝牙协议文档（从官方网页控制面板 JS 提取）

> 来源：
> - `W96D_V3.8.html`（new W96D / W96D Pro 风扇控制 V3.8，Web Bluetooth）
> - `W96P 快充任意流控制器.html`（W96D Pro 专属充电协议控制，Web Bluetooth）
>
> 提取方式：页面 JS 为未混淆明文，本文档内容为逐函数核对后的结果。

---

## 0. 通用约定

- 设备广播名前缀：`W96D`（`requestDevice({ filters: [{ namePrefix: "W96D" }] })`）
- 所有"hex 字符串"命令：把字节值格式化为大写十六进制字符串（如 `01`、`0064`），再 `TextEncoder` 编码后写入特征。即实际写入的是 **ASCII 十六进制文本**，不是原始字节！
- 多字节整数均为**大端**（BE）。
- 数值范围：转速 0-100%；定时 1-480 分钟。

## 1. GATT 服务与特征总表（new W96D / W96D Pro 风扇）

Base UUID：`0000xxxx-0000-1000-8000-00805f9b34fb`

### Service FFF0（主控服务）

| 特征 | UUID 后 4 位 | 读 | 写 | 说明 |
|---|---|---|---|---|
| POWER | FFF1 | u8 | hex字符串 | `00`=关机 `01`=开机。写前会自动关 Turbo/自然风 |
| TIMER | FFF2 | u16BE 秒（剩余） | hex字符串 4 位（=秒数） | 定时关机，分钟×60；`0000`=取消 |
| FAN_SPEED | FFF3 | u8（当前转速%） | hex字符串 2 位（转速%） | 写时若关机态会先写 POWER=01 |
| NATURE_WIND | FFF4 | u8 0/1 | `01`/`00` | 自然风开关；与 Turbo 互斥（开启一方前先关另一方） |
| SHUTDOWN_DELAY | FFF5 | u16BE 秒 | hex字符串 4 位 | 蓝牙休眠：0=永不；1-9 自动修正为 10；上限 65535 |
| GEAR_DOWN_MODE | FFF6 | u8 0/1 | `00`/`01` | 减档模式：0=逐级减档 1=直接回 0 |
| SPEED_CALIB | FFF7 | 4×u8 | hex字符串 8 位 | 档位 1-4 风速标定（每个 0-100） |
| TURBO_TIME | FFF8 | u16BE 秒 | hex字符串 4 位 | Turbo 持续时间；0=恢复默认 199 秒；0-600 |
| LIGHT_CONTROL | FFFA | u8 | `00`/`01` | `00`=一键关灯 `01`=恢复正常 |
| TURBO_REMAINING | FFFB | u16BE 秒 | - | Turbo 剩余秒数（>0 即 Turbo 生效中） |
| TURBO_MODE | FFFC | u8 | `01`/`00` | Turbo 开关 |

### Service FFD0（电源/电量服务）

| 特征 | UUID 后 4 位 | 读 | 写 | 说明 |
|---|---|---|---|---|
| BATTERY_INFO | FFD1 | 见下 | ASCII 文本 | 读：电压/电流/容量；写：`BAT_CAP=<mWh>,` 设置电池容量 |
| POWER_STATUS | FFD2 | 见下 | - | VBUS 电压/电流、充放状态 |
| MOTOR_INFO | FFD3 | 见下 | - | 电机电流/堵转/电压 |
| POWER_CONFIG | FFD4 | - | - | W96P 上复用为快充配置（见 §3） |

**BATTERY_INFO (FFD1) 读布局：**

| 偏移 | 类型 | 含义 |
|---|---|---|
| 0 | u16BE | 电池电压 mV |
| 2 | i16BE | 电池电流 mA（正=放电） |
| 4 | u32BE | 电池容量 mWh |

**POWER_STATUS (FFD2) 读布局：**

| 偏移 | 类型 | 含义 |
|---|---|---|
| 0 | u32BE | VBUS 电压 mV |
| 4 | i16BE | VBUS 电流 mA（`0x7FFF` 视为 0） |
| 7 | u8 | 充放状态：1=充电中，否则放电中 |

**MOTOR_INFO (FFD3) 读布局：**

| 偏移 | 类型 | 含义 |
|---|---|---|
| 0 | u16BE | 电机电流 mA |
| 2 | u8 | 堵转标志 |
| len-2 | u16BE | 电机电压 mV |

功率计算：电池功率 W = 电池电压(mV) × 电池电流(mA) / 1e6；电机功率同理。

**电池容量设置（ASCII 命令，写 FFD1）：**
`BAT_CAP=<mWh>,` 例：`BAT_CAP=61840,`（17200mAh × 3.6V ≈ 61840 mWh，页面按 容量mAh×电压V 计算）

### Service FFC0（SN 显示开关）

| 特征 | UUID | 写（ASCII） |
|---|---|---|
| BLE_SN_CONTROL | FFC1 | `BLE_SN=1,` 开 / `BLE_SN=0,` 关 |

### Service FFE0（自然风服务）

| 特征 | UUID | 读 | 写 |
|---|---|---|---|
| NATURE_WIND_CURVE | FFE3 | 128 字节 | 128 字节原始值（每个 0-100 风速%） |

- 曲线为 128 个点的风速序列，设备循环播放。
- 默认曲线：`55 48 40 33 28 22 21 26 33 41 48 54 58 60 61 58 52 45 37 30 24 20 25 33 40 48 53 57 60 60 56 51 43 36 29 23 21 28 37 47 56 63 68 71 72 71 67 62 54 46 36 29 23 20 27 37 48 57 64 69 73 74 76 78 80 82 84 86 88 90 89 87 83 77 70 62 53 43 34 27 21 20 26 32 38 43 47 49 50 48 44 38 33 27 24 20 21 26 31 37 42 46 48 47 42 36 31 27 23 20 22 27 33 39 44 47 48 46 41 36 30 26 23 20 22 27 33 38`
- "平滑/安静/强劲"只是网页端随机生成曲线的参数，不发给设备。

### Service FEE0（DFU / 设备信息）

写 FEE1，通知 FEE2。帧格式（也用于固件升级）：

```
偏移 0:   0x55 帧头
偏移 1:   key（请求方用 0；XOR 掩码索引）
偏移 2-3: payload 长度（小端 u16，先与 CRC8_TABLE[key] 异或后发出）
偏移 4..: payload（逐字节与 CRC8_TABLE[key] 异或后发出）
最后一字节: CRC8(多项式 0x3B，初始值 0x89)，对"未异或的原始帧"（头+key+长度+payload）计算
BLE 分块：每包 ≤197 字节，writeWithoutResponse
```

已知 payload 命令（真机实测，设备 W96D_000123 / V1.3；响应 payload[0]&0x7F = 总长校验）：

| 发送 payload | 实测响应 | 含义 |
|---|---|---|
| `8A` | `04 0d`（v10 = 主版本×10+次版本） | 读固件版本（`0d` = V1.3） |
| `0F` / `8F` | `0a 7b 00 00 00`（SN 小端 u32 = 000123） | 读序列号（设备名 `W96D_000123` 的后缀即此值）。同一命令的两种帧法，应用态发 `0F`、带 bit7 发 `8F` 都回 SN |
| `85`（CHECK_IN_DFU）| 应用模式 `01`(NG) / 传输阶段 `02`(OK) | 查询当前是否在 DFU/OTA 阶段 |
| `84`（ENTER_DFU）| `02`（约 87ms），**应答后设备立即失联** | 进入 DFU 阶段的前置命令 |
| `8F`（切换命令）| **无应答** | ★ 必须紧跟在 `84` 的应答之后（实测 59ms）、**在同一条连接上**发出；设备收到后立刻停掉 BLE，链路约 5s 后以 reason `0x08`（监督超时）断开 |
| `0B`（RESET）| 无应答 | 复位回应用模式 |
| `8C`（GET_PAGE_SIZE）| `05 00 10`（LE16 = 0x1000 = 4096） | 读 flash 页大小 ⇒ 写块 = 128B |
| `81` + 固件头 96B（整帧 102B）| `02` | 解锁/下发文件头（**只在重连后的传输阶段被接受**）|
| `87`（START_UP）| `02`（约 1330ms 后才到）| 开始接收数据（设备在擦除） |
| `02` + 128B 数据（整帧 134B）| 无逐块回执 | 写 flash，地址由设备自增；末帧可为短帧（V1.3 末帧 100B，整帧 106B）|
| `88`（END_UP）| `02`（约 133ms）| 数据结束 |

**抓包实测的关键参数（2026-09-25，`BT_HCI_*.cfa`，官方小程序成功刷机）**

| 项 | 应用态固件 | 传输（DFU）阶段固件 |
|---|---|---|
| MTU 协商（手机请求 247）| 应答 **243** | 应答 **247** |
| `85` 应答 | `01` | `02` |
| 写特征 ATT 句柄 | 0x0010（写）/ 0x0012（通知，CCCD 0x0013） | 同左（但**必须重连并重新 `startNotifications()`**）|
| 大帧 | 约 18~20B 以上静默 | 单条 ATT 写 102B / 134B 均正常 |

- 全程使用 **ATT `WRITE_CMD`（无响应写）**，包括控制帧；所有帧 **key=0**。
- 设备广播全程不变（名 `W96D_000123` + 服务数据含 ASCII `|DFU`）⇒ **`|DFU` 不是"已进 DFU"的判据**，重连也**不需要重新扫描**（同一地址直连）；判据只有 `85` 的应答和 MTU 应答值。
- 数据阶段节奏：639 帧 / 81764B，帧间隔 min 13ms / 中位 18ms / max 24ms，**54.8 帧/秒**，全程 11.66s。

**实测要点（对刷机实现至关重要）**
- **key 必须为 0**：非 0 key 时连 6~10B 小命令都被设备静默（无任何应答）；key=0 时全部正常。官方 APK 的 `PACKAGE_DEBUG_MODE=true` 使 `selectTxKey()` 恒返回 0，与网页版一致。
- **"大帧静默"的真相是切换没成功**：抓包证明小程序发的就是 102B/134B 的**单条整帧**、字节与我们的完全一致。此前我们在应用态固件上发 DFU 数据帧，旧固件只有约 20B 收包缓冲 ⇒ 静默；传输阶段固件对同样的帧回 `02`。详见《逆向分析与统一工具方案.md》§2.4.0。

> ✅ **OTA 已内置**（2026-09-25，工具 v20260925b）：设备信息页「⬆ 固件升级 OTA」，含 **🧪 干跑（不擦除）** 模式；流程与实测参数见上表与《逆向分析与统一工具方案.md》§2.4.0。首次真机验证可用 `rom/W96D_V13.up`（抓包证明同版本可重刷）。

## 2. W96P 快充任意流控制器（W96D Pro 专属）

Service FFD0（同上）。

| 特征 | UUID | 用途 |
|---|---|---|
| BATTERY_INFO | FFD1 | 读：u16BE 电压 mV @0，i16BE 电流 mA @2，u32BE 容量 @4 |
| POWER_Q1 | FFD2 | 读实时 + 写 `POW_C_IN=<0|1>,`（C 口输入快充开关） |
| POWER_Q2 | FFD4 | 读配置 + 写 `POW_1A=<hex>,` / `POW_1C=<hex>,` / `POW_1D=<hex>,` |

**POWER_Q1 (FFD2) 读布局：**

| 偏移 | 类型 | 含义 |
|---|---|---|
| 0 | u32BE | VBUS 电压 mV |
| 4 | i16BE | VBUS 电流 mA（`0x7FFF`→0） |
| 6 | u8 | powC（输出使能：1=开启） |
| 7 | u8 | powSta（协议状态） |
| 8 | u8 | powCOut |
| 9 | u8 | powCIn |
| 10 | u8 | powCHi |

**POWER_Q2 (FFD4) 读布局：**

| 偏移 | 类型 | 含义 |
|---|---|---|
| 1 | u8 | powVer（协议版本） |
| 2 | u8 | powSink |
| 3 | u8 | powSrc |
| 6 | u8 | pow1a |
| 7 | u8 | pow1c |
| 8 | u8 | pow1d |
| 9 | u8 | pow1e |
| 13 | u8 | pow2a |
| 14 | u8 | pow2b |
| 15 | u8 | pow2c |

**写命令（ASCII，实际发送 `命令 + ","`）：**
- `POW_C_IN=<v>,` → FFD2。0=恢复默认（默认 POW_C_IN=0）
- `POW_1A=<hex>,` → FFD4。bit2：0=支持 12V 输入，1=不支持。默认 0x1C
- `POW_1C=<hex>,` → FFD4。bit4：PD Sink 使能（0=使能）。默认 0x00
- `POW_1D=<hex>,` → FFD4。Sink 协议位掩码：bit7 FCP、bit3 AFC、bit1 SCP（0=使能）。默认 0x00

写后验证方式：回读对应特征确认。命令节流 300ms，失败重试 3 次。

## 3. 与老款 W96D（USB 数据线版）的关系

老款走 USB HID（DeepFanController，Flutter 应用），其协议字符串已发现同风格 ASCII 命令：
`GEAR=0,`、`WIND_IDX`、`WIND_TIM`、`WIND_CLR`、`WIND_SAVE`、`WIND_SUM`。
（详细帧格式见反汇编分析。）
