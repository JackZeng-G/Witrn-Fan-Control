# WITRN 风扇系列逆向分析与统一工具可行性报告

> 分析对象：
> 1. **老 W96D**（红外/USB 线版）：`DeepFanController.zip`（Windows Flutter 应用，作者"期门弋猎绝"）+ 官方安卓版 `w96d.apk`（用户从手机导出，即 `w_fan_a`）
> 2. **New W96D**（蓝牙版）：`W96D_V3.8.html`（Web Bluetooth 网页面板）
> 3. **W96D Pro / W96Pro**（蓝牙版+快充魔改）：同 New W96D 面板 + `W96P 快充任意流控制器.html` + 官方安卓 APP `neww96d.apk`（= 官网 `W66D_APP.zip`，OTA+遥控）

---

## 一、结论：三个版本全部可以逆向，且可以整合成一个工具

| 版本 | 通信方式 | 软件形态 | 逆向难度 | 协议还原度 |
|---|---|---|---|---|
| New W96D / W96Pro | BLE (Web Bluetooth) | 网页 JS（未混淆） | ★☆☆☆☆ 直接读源码 | **100%** |
| W96D Pro 快充控制 | BLE | 网页 JS（未混淆） | ★☆☆☆☆ 直接读源码 | **100%** |
| 老版 W96D (安卓) | USB (bulk) | Flutter ARM64 (Dart AOT) | ★★☆☆☆ Blutter 反编译 | **~95%** |
| 老版 W96D (Windows exe) | USB (HID) | Flutter x64 (Dart AOT) | ARM64 版交叉印证 | **~90%** |

关键发现：**三个版本本质上是同一套 ASCII 命令协议**（`命令=值,` 格式），只是传输层不同（BLE 特征写入 / USB bulk / USB HID）。统一工具完全可行。

---

## 二、各版本协议详解

### 2.1 老版 W96D（USB 数据线）

**硬件识别：**
- 已知两批硬件 ID：`0x0716:0x5103`（**DeepFan**，实测：HID 类，无 reportId，64 字节中断 IN#2/OUT#1）与 `0x0E2C:0xA206`（官方安卓 APK 内置，另一批次）
- 安卓端：`UsbManager` 枚举 → `claimInterface` → **bulk 传输**（OUT 端点写 / IN 端点读，端点按 `getDirection()` 0=OUT / 0x80=IN 自动区分）
- Windows 端：hidapi（hid4flutter 插件），走 HID 类驱动——设备为 HID 类（中断端点），Windows 下 WebUSB 无法认领，Web 通道须用 WebHID
- **浏览器直连结论（安卓真机 0x0716:0x5103 实测）**：WebUSB 在所有平台都禁止声明 HID 类接口（"protected class" 硬性限制，与端点 bulk/interrupt 无关）。桌面 Chrome/Edge 的 WebHID 是唯一常规 Web 通道。**安卓 WebHID 状态**：Chrome 官方计划在 **Chrome 157** 支持（chromestatus #5172464636133376），当前仅 Canary/Dev 有 `web-hid-on-android` flag，正式版 flags 里无此项。工具已按能力检测适配——等 157 正式发布（或装 Canary 开 flag），安卓上「连接HID」按钮自动出现。官方安卓 APK 能用是因为原生 USB Host API 不受浏览器保护类限制

**下行（控制命令）**：纯 ASCII 文本，格式 `命令=值,`（无帧头、无校验、无填充）：
```
GEAR=<n>,          档位。实测（0x0716 批次真机验证）：值直通——n=0 停止，n=1/2/3/4 = 第1/2/3/4档
                   遥测 Gear 与命令值同值显示（四档风扇，0-4 共 5 个状态）
GCLR,              清除档位 PWM 设置（值=0）
GSET1..4=<pwm>,    各档 PWM 设置（1-100），直发验证通过
WIND_SUM=<组数×10>, 自然风总点数：设备固定 10 点/组，组数 1-20 → WIND_SUM = 10..200；官方抓包 10 组 → WIND_SUM=100?（实测抓包值 10 对应 1 组）
WIND_TIM=<秒>,     整条曲线的总时长 Time Cycle，1-3600 秒直发（官方抓包 3600）；每点 = TIM÷总点数 秒
WIND_IDX=<i×256+值>, 逐组写入风量值：组号 i×256 + 风量(0-100)；官方抓包 10 组全 40：40,296,552,808,...,2344
WIND_SAVE=0,       保存曲线（上传序列最后一条）
WIND_CLR=0,        恢复默认曲线（官方"重置默认"按钮即发此条）
```
- 注意：设备为**四档**（1-4）+ 停止（0），早期日志曾误判为"差一映射"，系测试时残留档位与时序干扰，已由第二轮真机验证纠正（GEAR=2→Gear=2、GEAR=3→Gear=3）
- 已排除：GEAR=5 作为老版自然风开关（真机无效）。老版自然风模式的启动方式未定——候选：上传曲线后设备自动进入，或依赖官方 exe 的 `GEAR=0,` 前缀时序；新版 BLE 用专门的 FFF4 开关，无此问题

**自然风曲线结构（真机确认，仅老版 USB/HID 协议——新版 BLE 不适用）**：设备按 **10 点/组** 组织，**组数上限 20**（总点数上限 200）。老软件的"Point per Group"就是每组点数（固定 10），组只是顺序编号。**Time Cycle = 整条曲线（全部点）走完一圈的总时间**（如 100 点 + 3600s → 每点 36 秒），不是单组或单点时长。WIND_SUM = 组数×10（总点数）。128 点真机无效，100 点（10 组）可用。发送流程：`WIND_SUM=<组数×10>,` → `WIND_TIM=<总秒数>,` → 逐条 `WIND_IDX=<序号×256+值>,` → `WIND_SAVE=0,`；`WIND_CLR=0,` 恢复默认

**上行（遥测）**：设备约 100ms 持续推送一行逗号分隔 ASCII 文本（**实测 0x0716 批次**，行首带逗号）：
```
,SN=000123,Gear=0,Speed=0,VBus=5.0,VBat=4.2,IBat=0.00
```
字段：SN 序列号 / Gear 当前档位 / Speed 转速 / VBus 输入电压 V / VBat 电池电压 V / IBat 电池电流 A（充电为正）。UI 显示项 Vpwr/Vbat/Ibat/PWM/Gear 与之对应。

### 2.2 New W96D（BLE，W96D Pro 通用）

设备名前缀 `W96D`，完整协议见 `协议文档_W96D_BLE.md`，要点：
- Service `FFF0`：FFF1 开关机 / FFF2 定时 / FFF3 转速 / FFF4 自然风 / FFF5 休眠 / FFF6 减档模式 / FFF7 档位标定 / FFF8 Turbo 时长 / FFFA 灯光 / FFFB Turbo 剩余 / FFFC Turbo 开关
- Service `FFD0`：FFD1 电池(读)+容量设置(ASCII `BAT_CAP=<mWh>,`) / FFD2 电源状态 / FFD3 电机
- Service `FFC0`：FFC1 `BLE_SN=1,`/`BLE_SN=0,`
- Service `FFE0`：FFE3 自然风曲线 128 字节直写（新版机制与老版 WIND_* 命令完全不同，无组概念）
- Service `FEE0`（DFU/信息）：帧格式 `55|key|len^mask|payload^mask|CRC8`，CRC8 多项式 **0x3B** 初始值 **0x89**；命令 0x8A 读版本、0x8F 读 SN
- 写入值大多为 hex 文本（如转 50% 写 `"32"`），多字节大端

### 2.3 W96D Pro 快充任意流控制（BLE FFD0 服务）

- FFD2 (POWER_Q1)：读 VBUS 电压/电流 + 输出使能；写 `POW_C_IN=<0|1>,`
- FFD4 (POWER_Q2)：读快充配置；写 `POW_1A=<hex>,`（bit2 12V 支持）、`POW_1C=<hex>,`（bit4 PD Sink）、`POW_1D=<hex>,`（bit7 FCP/bit3 AFC/bit1 SCP）
- FFD1 (BATTERY_INFO)：电压 u16BE / 电流 i16BE / 容量 u32BE
- 命令以 ASCII 发送且**带逗号后缀**，写后回读验证，300ms 节流

### 2.4 官方安卓 APP（neww96d.apk）的 DFU/OTA 协议（BLE FEE0）

> **状态（2026-09-25 更新）**：OTA 已按抓包**重新实现并内置**到工具（v20260925b，设备信息页「⬆ 固件升级 OTA」，含"干跑"模式）。**先读 §2.4.0 定论**（逐条时序、四条决定性结论、可照抄流程、工具实现与 mock 回归）。§2.4.0-历史 是 09-24 的探索留痕，其中 CH592F `IAP_LEN=20` 等假设已被抓包**推翻**，只看结论即可。

帧协议与网页版一致（`55|key|len|payload|CRC8`，XOR 掩码=CRC8_TABLE[key]），完整命令表：
- **key：官方 APK 恒为 0**（`BleConstants.PACKAGE_DEBUG_MODE=true` → `BlePackageProtocol.selectTxKey()` 直接 `return 0`；动态 key 分支在此 APK 中不会走到），网页版同为 0；真机实测非 0 key 连小帧都被静默
- ACK 位：payload[0] bit7 (0x80) = 需要 ACK
- 控制命令：0x1 NG / 0x2 OK / 0x4 ENTER_DFU / 0x5 CHECK_IN_DFU / 0x7 START_UP / 0x8 END_UP / 0xA GET_VERSION / 0xB RESET / 0xC GET_PAGE_SIZE / 0xF GET_SN
- 数据命令：0x1 REQ_UNLOCK / 0x2 WRITE_FLASH / 0x4 VERSION / 0x5 PAGE_SIZE / 0xA SN
- 写 flash：分块 ≤0xFC 且 4 对齐，`0xFF` 填充，page size 从设备读取（LE u16）；官方写块命令 `buildDataPayload(0x2, false, chunk)` —— **不带 ACK 位、不带地址**（地址由设备端自增）
- 流程（APK）：GET_VERSION → CHECK_IN_DFU（NG→ENTER_DFU→等设备重启断链→重连→CHECK_IN）→ GET_PAGE_SIZE → REQ_UNLOCK(`0x81+固件头96B`) → START_UP（擦除）→ WRITE_FLASH×N（块间 5ms，无逐块回执）→ END_UP → RESET（无应答）

#### 2.4.0 ★ 定论：官方小程序成功刷机的完整抓包复盘（2026-09-25）

**证据**：用户用微信小程序完整刷完一次（安卓手机），HCI 日志 `BT_HCI_*.cfa`（354KB / 4940 包 / 106.3s）。一条命令即可复现下面全部结论：

```bash
node tools/parse_hci_log.js "BT_HCI_*.cfa"
```

（btsnoop 头 16B + 记录头 24B，datalink 1002；ACL→L2CAP→ATT；设备通知帧按 `CRC8_TABLE[key]` 解掩码后 **CRC 全部校验通过**。抓包时段共 4 次连接，前 3 次为识别/准备，第 4 次是真正的传输阶段。）

**实测时序**（相对文件起点，连接句柄 0x0201，写操作**全部**是 `WRITE_CMD` 无响应写，帧格式 `55 00 | len16LE | payload | CRC8`）

| 时刻 | 发送 | 设备应答 | 说明 |
|---|---|---|---|
| 68749 | — | 第 3 次连接建立 | 对端 `aa:bb:cc:dd:ee:ff` |
| 70115 / 70207 | MTU 请求 247 | **MTU 应答 243** | 应用态固件上限 243 |
| 71389 / 75556 | CCCD 0x0013 ← `01 00` ×2 | — | 订阅 FEE2 通知 |
| 75763 | `8F` GET_SN | `0a 7b 00 00 00` (+100ms) | 序列号 |
| 75921 | `8F` GET_SN | 同上 (+88ms) | 初始化 2/5 |
| 76067 | `8A` GET_VERSION | `04 0d` (+88ms) | 初始化 3/5 |
| 76213 | `85` CHECK_IN_DFU | `01` = **应用态** (+88ms) | 初始化 4/5 |
| 76360 | `84` ENTER_DFU | `02` (+87ms) | 初始化 5/5 |
| **76506** | **`8F`「切换命令」** | **无应答** | ★ 只比 `0x84` 的应答晚 **59ms**，同一条连接 |
| 81475 | — | 断开，原因 **0x08 = 监督超时** | 本机**未**发 HCI Disconnect；设备从 76475 起已不再响应（5s 超时） |
| 82035 | 重连（同一地址，无需重新扫描） | — | 设备广播内容**没变**（见下） |
| 83285 / 83370 | MTU 请求 247 | **MTU 应答 247** | 固件已换（对比前三次的 243） |
| 83386 / 83525 | CCCD 0x0013 ← `01 00` ×2 | — | **重新订阅** FEE2 |
| 83737 | `85` CHECK_IN_DFU | `02` = **DFU 态** (+72ms) | 传输阶段确认 |
| 83823 | `8C` GET_PAGE_SIZE | `05 00 10` = 4096 (+84ms) | 块大小取 128B |
| 83920 | `81` + 96B 文件头（整帧 **102B**） | `02` (+84ms) | ★ **大帧被接受** |
| 84917 | `87` START_UP | `02` (+1330ms) | 设备要 ~1.3s 准备 |
| 86576→98233 | `02`+128B ×638，末帧 `02`+100B（134B/106B） | 无逐块回执 | **639 帧、共 81764B**，间隔中位 18ms、54.8 帧/秒 |
| 98254 | `88` END_UP | `02` (+133ms) | |
| 98418 | `0B` RESET | 无应答 | 设备重启 |
| 103459 | — | 断开（reason 0x08） | 升级完成 |

**五条决定性结论**

1. **"切换"是紧跟在 `0x84` 之后、在**同一条旧连接**上补发的那条 `0x8F`，而且必须"不等重连"**。抓包里它比 `0x84` 的应答只晚 59ms，之后设备再无任何应答、链路 5s 监督超时断开。⇒ 我们此前的偏差正在这里：我们**等断链/重连之后**才发 `0x8F`，设备那时已不在"刚应答完 0x84"的状态，于是把 `0x8F` 当普通 SN 查询回了个 `0a+SN`（见 00:03 真机日志），**真正的切换从未发生**，后续 102B 解锁帧自然被静默丢弃。
2. **大帧完全合法，`IAP_LEN=20` 假设被推翻**。小程序用**单条 ATT 写**发 102B 解锁帧（ACL datalen 109 = 4+3+102）和 134B 数据帧（datalen 141 = 4+3+134）。⇒ 帧长、写入类型、"浏览器发不出大帧"全都不是原因；应用模式下那条 18~20B 界线只是**旧固件**的收包缓冲，与传输阶段无关。
3. **重连后必须重新订阅 FEE2**（写两次 CCCD 0x0013 才发 `0x85`）。两种模式下 ATT 句柄相同（写 0x0010 / 通知 0x0012 / CCCD 0x0013），但旧连接的 characteristic 对象不可复用。
4. **官方长帧的 CRC 与官方网页表不一致**：小程序用 poly=0x3B 真值表，网页表 120/202 两处错值只影响长帧 ⇒ 详见 §2.4.0.1（这才是 102B 解锁帧长期静默的真凶）。
5. **重连不需要重新扫描**：设备广播全程不变（`W96D_000123` + 服务数据 `…2657393644303031403130 7c 444655 23`，即含 ASCII `|DFU`）——**`|DFU` 标记在应用态就存在，不能当"已进 DFU"的判据**；判据只有 `0x85` 的应答（`01` 应用 / `02` DFU）与 MTU 应答（243 / 247）。

**正确流程（恢复 OTA 时照此实现）**

```
连接 → 0x0F/0x8F GET_SN → 0x8A GET_VERSION          # 识别（可选，小程序为 8F,8F,8A）
     → 0x85 CHECK_IN_DFU → 期待 01（应用态）
     → 0x84 ENTER_DFU   → 期待 02
     → 0x8F「切换」      → 立刻发（≤100ms、同连接、不等重连、无应答）   ★关键
     → 等 gattserverdisconnected（设备侧 5s 监督超时；若 6s 未断则主动 disconnect）
     → 重连（500ms 起退避）→ 重新 getPrimaryService/getCharacteristic → startNotifications()
     → 0x85 → 期待 02（传输阶段确认）
     → 0x8C → 05 00 10（页大小 4096 ⇒ 块 128B）
     → 0x81 + 96B 文件头 → 期待 02
     → 0x87 → 期待 02（约 1.3s 后到达）
     → 0x02 + 128B ×638 + 末帧 100B（无逐块回执，≈18ms/帧）
     → 0x88 → 期待 02
     → 0x0B RESET（无应答，设备重启）
```

分块公式：数据总长 = 文件长度 − 96（V1.3：81860 − 96 = **81764**）；块数 = `ceil(81764/128)` = **639** = 638 满帧 + 1 个 100B 尾帧。**页大小必须用 `0x8C` 的真实应答（0x1000）**，不要写死。

**与上一次差点结论的差异（供复核）**：09-24 我们以为"关键在 DFU 模式下补发 `0x8F`"——抓包证明顺序是「`0x84` → **同连接立刻 `0x8F`** → 等断链 → 重连」，而我们先断链重连、后发 `0x8F`，那一步就已经无效了。

#### 2.4.0.1 ★★ 真正的根因：CRC8 表里两个错值（2026-09-25 真机跑通后由抓包反查出来）

真机把切换时序修好之后，`0x84/0x8F` 切换、重连、`0x85→02`、`0x8C→05 00 10` 全部正常，**但 102B 解锁帧仍然两次零应答**。把"我们发出的帧"和"抓包里小程序发出的帧"逐字节比对：

```
差异字节位置: 0x65
  @0x65: 抓包=20 我们=4e          ← 只差最后 1 个字节（CRC）
payload 97B 是否一致: true
```

随后拿抓包里 **657 条帧**当样本反查，发现：

| 帧类型 | 工具原表（= 官方网页表） | poly=0x3B 真值表 |
|---|---|---|
| 6B 控制帧（17 条） | 17/17 | 17/17（两表在这些输入上恰好同值） |
| 102B 解锁头 | **0/1** | 1/1 |
| 106B 尾帧 | **0/1** | 1/1 |
| 134B 数据帧（638 条） | 251/638 | **638/638** |

原因：官方网页 `W96D_V3.8.html` 的 CRC8 表里，**下标 120 与 202 两处被误写成 `200`**（正确值 `102`/`101`，都是复制粘贴错）。工具当初"以官方为准"照抄了这张表 —— 而**官方小程序用的是数学上正确的 poly=0x3B（非反射/MSB-first，初值 0x89）表**。短帧的运算路径恰好不会踩到这两个错项，所以：

- 所有 6~10B 控制命令（`8A/8F/85/84/8C/87/88/0B`）**一直都对**，设备也一直应答 ⇒ 我们从没怀疑过 CRC；
- 一旦载荷变长，运行中的 CRC 值就会命中 120/202 ⇒ **CRC 算错 ⇒ 设备静默丢弃**。这正是困扰整晚的"102B 解锁帧怎么发都没反应"：**不是帧长、不是写入类型、不是时序**，而是校验字节错了。

**修正**：`W96D统一控制台.html` 与 `tools/crc8_table.js` 的这两项改为真值（120→102、202→101），并加注释说明来历。

**验收（黄金帧测试 `tools/tests/golden_frame.test.js`）**：用抓包里小程序真实发出的**每一帧**，与工具 `dfuPack()` 生成的结果逐字节比对 ——
`102B 解锁帧 ✓ 逐字节一致｜638 × 134B 数据帧 638/638 ✓｜106B 尾帧 ✓` —— **帧布局 + CRC 算法与成功刷机完全一致**（修正前长帧 0/640）。

**真机复验（2026-09-25 01:08）**：修正后 `0x81+头96B` **首次拿到 `02`**（工具发出的 102B 帧与抓包逐字节一致）、`0x87→02`、639 块全部发送完毕（81764B / 14.5s）。此前整晚"102B 无论怎么发都静默"从此终结 —— 确认是 CRC 而非时序/长度/写入类型。

**★★ 真机刷机成功（2026-09-25 01:32）**：`0x81+头96B → 02`、`0x87 → 02`、639 块（81764B / 13.3s / 6.0KB/s）、**`0x88` 第 2 次重试拿到 `02`**、`0x0B` 复位后设备重启断链。整条链路（切换 → 重连 → 解锁 → 写块 → 收尾 → 复位）在真机上首次全通。

**收尾环节的加固（v20260925e / f）**：最后一次实测中 `0x88`（结束传输）10s 内无应答。设备收尾（写最后几页 flash + 校验）可能远慢于抓包的 133ms，故：`0x88` 最多重发 3 次 × 每次等 20s；期间设备自行断链即判为"重启＝完成"；彻底无应答时反问 `0x85` 判定（`02`＝仍在传输阶段/写入未被接受，`01`＝已回应用态/固件生效）；最后自动重连回读版本 `0x8A` 并显示结论。另加「帧间隔」输入（默认 20ms，可放慢到 30~40ms 以排除丢块）。

> 附带确认：短帧的新旧表 CRC 完全相同（`0F/8A/85/84/8F/8C/87/88/0B/81/01/02` 全部不变），所以这次修正**不会影响任何已经正常工作的命令**；设备侧只用正确表（否则它不会接受小程序的帧）。

**工具已实现该流程（`W96D统一控制台.html` v20260925g，设备信息页 → ⬆ 固件升级 OTA）**

| 项 | 说明 |
|---|---|
| 入口 | 设备信息页 → **⬆ 固件升级 OTA** → 选 `.up` → 「🧪 干跑（不擦除）」或「⬆ 开始升级」 |
| 干跑 | 只走到"已进入传输阶段"（`0x84`→立刻 `0x8F`→重连→`0x85→02`→`0x8C`），**不发 `0x81/0x87/0x0B`，不擦除** |
| 升级 | 继续 `0x81+头96B`（必须回 `02`）→ `0x87`（**此步擦除**）→ 639 帧 × 128B（无逐块回执，按 18ms/帧）→ `0x88`（必须回 `02`）→ `0x0B` 复位 |
| 安全 | 任何一步失败都会**明确报出"是否已擦除"**：`0x87` 之前失败 ⇒ 设备应用区完好，直接重试即可；`0x87` 之后失败 ⇒ 设备停在 DFU 阶段，重新连接后再刷 |
| 型号/版本 | 读头部 `[0x21]` 型号（须 `W96D001`，不符则弹窗确认）与 `[0x4d]` 版本字节；页大小取自 `0x8C` 真实应答 |
| 回归 | mock 全流程（`tools/tests/ota_flow.test.js`，用**真实固件字节**）：写入 650 条、数据 639 帧、**拼接后与固件体 81764B 逐字节一致**、首/末帧 134/106B、`0x0B` 为最后一帧、干跑阶段无擦除类命令，并断言 `0x8A/0x0F/0x8C` 应答真的被消费（不是走兜底值）——全部断言通过 |

**首次真机试跑暴露的第 4 个坑：应答帧的末尾校验字节（v20260925c 修复）**

真机日志：

```
[00:51:59] >> [DFU] cmd=0x85 帧=55 00 01 00 85 6b
[00:51:59] [RX] ⚠第 CRC 校验失败，丢弃一帧：key=0x9 len=1 payload=02
[00:52:02] !! OTA 异常/中止: DFU 超时
```

解出来的 `len=1 / payload=02` **正是我们要的"已在传输阶段"确认**，掩码、长度、载荷三者自洽，唯独最后一个校验字节与我们的 CRC8 算法不符 —— 而 `onDfuReceive` 当时对 CRC 不符是**直接丢弃**，于是设备明明确认了，工具却报"超时"并中止。

> 为何可以宽容：BLE 链路层已有 CRC16；帧结构（`0x55` 头 + 合法长度 + 完整载荷）与掩码自洽时，帧本身是可信的。抓包里 16 条设备通知的 CRC **全部通过**，说明这是个别帧/个别时序下的问题，不该让整个刷机流程陪葬。

修法：① CRC 不符但结构完整 ⇒ **宽容采用**并显著留痕（原始通知字节 + 期望/实际 CRC）；② 重连时的 `0x85` 校验结果复用（`S.ble.lastCheckIn`），不再重复发一轮——那次重复正是踩坑点；③ `0x8C`/`0x81` 各加一次重试（非破坏性）；④ 超时打印"残留半帧"（已收字节数 + 最近通知原始字节）。回归：`tools/tests/ota_crc_lenient.test.js`（故意写错应答的 CRC 字节 ⇒ 断言仍取到载荷且 OTA 全流程跑完 639 帧）。

**两个额外实证（都来自这份抓包）**
1. **同版本可以重刷**：小程序刷的固件头部 `[0x4d] = 13`（V1.3），而设备当时也回 `04 0d`（V1.3）⇒ 官方 bootloader **不拒绝同版本**，所以用本仓库 `rom/W96D_V13.up` 直接做首次真机验证即可，不需要"版本号 +1"的测试包。
2. **抓包刷的就是我们手里这份固件**：解锁帧里的 96B 头部与本仓库 `rom/W96D_V13.up` 的头部**逐字节完全一致**（32 字符校验串 `105a109580404617889a6d116bbe7896` + `W96D001` + …）。

---

#### 2.4.0-历史 OTA 探索记录（2026-09-24，**部分假设已被上面的抓包推翻，仅作留痕**）

**一句话（当时结论）**：设备在应用模式/DFU 模式下都正常应答所有**小帧**命令，但**拒绝解析我们发出的"大帧"**；在试遍所有已知维度后仍无法定位正确形式，OTA 暂时从工具中移除，待拿到官方小程序的真实字节再继续。

**★ 2026-09-24 23:5x 第二次录屏（逐帧放大）读出小程序的真实命令序列 —— 我们此前两条路都错在顺序上**

```
DFU 初始化 1/5 已发送 0x8f
DFU 初始化 2/5 已发送 0x8f        ← 0x8F 连发两次
DFU 初始化 3/5 已发送 0x8a        ← 读版本
DFU 初始化 4/5 已发送 0x85        ← CHECK_IN_DFU
DFU 初始化 5/5 已发送 0x84        ← ENTER_DFU（设备重启进 DFU）
发送切换命令 0x8F，等待设备进入传输阶段...   ← 关键：0x8F 是在 0x84 **之后**、且处于 DFU 模式时才发的
设备未自动断开，主动断开旧连接...            → 主动断开旧连接
设备正在切换到升级模式，请稍候...            → 重连
FEE2 notify 已订阅
确认 OTA 传输阶段...                         ← 再发 0x85（02=已在 DFU/OTA）
发送 0x8C (DFU 就绪)                         ← 页大小 0x1000 ⇒ 块 128B
头部已发送，等待设备确认... → 设备已接收头部 ← 解锁帧 0x81+头96B 被 ACK
已发送 0x87 (开始数据)，等待设备准备... → 设备已准备接收数据
数据块 N/639 … → 升级完成！设备重启中...
```

**对比我们此前两条路线的错处**：
- **APK 路线（0x84）**：只做了 `0x8A → 0x85 → 0x84 → 重连 → 0x85 → 0x8C → 0x81+头96B` ——**缺了开头的两个 `0x8F`，也缺了 0x84 之后的那次 `0x8F` "切换命令"**；
- **小程序路线（0x8F）**：把 `0x8F` 发在了**应用模式**（0x84 之前）——顺序反了，所以设备不切换、重连后仍是应用模式。
⇒ 正确顺序是"**先 0x84 进 DFU，再在 DFU 模式下发 0x8F 切换**"，这才是设备接受解锁头的前提。

**00:00 首跑结果（build v20260924b）与一个必须修的 bug**：初始化 5 步全部按预期（`0x8F→SN`、`0x8F→SN`、`0x8A→04 0d`、`0x85→01`、`0x84→02` → 断链 → 重连 → `CHECK_IN=02`），DFU 模式下 `0x8F` 也拿到 SN 应答（`0a 7b 00 00 00`）；**但强制断开后的重连 1 秒内就失败退出**。

根因（代码 bug，非协议问题）：`cleanupBle()` 会把 `S.ble.device` 置空，而 `gattserverdisconnected` 事件**早于** `dfuWaitReconnect()` 执行 ⇒ 函数取到的 `dev` 是 `null`，循环第一轮 `if (!dev || !dev.gatt) break;` 直接退出（本该重试 25×1.2s）。第一次重连（`0x84` 之后）之所以成功，是因为那时设备尚未断链、`S.ble.device` 还在。

修复：① `cleanupBle()` 先把句柄存进 `S.ble.lastDevice` 再置空；② `dfuWaitReconnect(devOverride)` 用 `devOverride || S.ble.lastDevice || S.ble.device` 兜底，取不到时**明确报错**而非静默 break，并打印 25 次重试的最后错误；③ 强制断开前先捕获 `devBefore` 并把它传给重连。mock（模拟 `disconnect()` 走真实 `cleanupBle()`）复测：两次重连均成功，最终 `★★ 解锁被设备确认`。

**00:03 第二跑：序列完整复现，但解锁仍静默 —— 记录与下一步**

修好重连 bug 后，序列**逐条对上小程序**：`0x8F→SN`、`0x8F→SN`、`0x8A→04 0d`、`0x85→01`、`0x84→02` → 断链 → 重连 → `CHECK_IN 02` → **DFU 模式下发 `0x8F`→SN** → 主动断开 → 重连（`DFU_W/DFU_N` 均取到）→ `CHECK_IN 02` → `0x8C→05 00 10` → 头部 `0x81+96B`（带响应写）→ **静默**；无响应写复测 → **静默**。

⇒ 结论：**"DFU 初始化 + DFU 模式下的 0x8F 切换"这套序列并不足以让设备接受解锁帧**（或至少不足以让它对本帧应答）。录屏新读到开头还有两条：`已识别固件型号：W96D`、`FEE2 notify 已订阅`（后者我们在 `bleDiscover` 里已做）。

**00:06 第三跑：帧长彻底排除** —— 在正确序列之后的传输阶段做帧长探测，**21/41/61/81/101/102/121/145B 全部有应答**（都是 `01`=NG，说明设备解析了每一帧）⇒ **帧长不是解锁静默的原因**（我们的解锁帧 102B 远未超限）。结论把问题锁定在**解锁帧的内容/命令本身**。

**同时补了两项留痕/探测（因为发现自己可能在"盲看"）**：
1. **RX 丢帧留痕**：`onDfuReceive` 此前对 **CRC 校验失败**与**长度字段非法**的帧**静默丢弃、不留任何日志** —— 也就是说"设备其实回了、只是被我们丢掉"这种情况我们一直看不见。现在两种丢弃都会写日志（`[RX] ⚠第 CRC 校验失败…` / `[RX] 长度字段非法…`），并打印 key/len/payload。
2. **`0x81` 载荷长度扫描**：在正确的传输状态下，用真实固件头按 1/8/16/32/48/64/80/96/97B 逐个试 `0x81`，找"设备认的解锁长度"（`0x81` 非破坏性，裸发只回 NG）。结论文案会区分四种情形：解锁被确认 / 帧长超限 / 载荷长度不对 / 命令本身被忽略。

**00:14 bug 报告解析结果：这次没抓到（HCI 开关未生效/未落盘）**

用户在手机上生成了完整 bug 报告 `bugreport-PD2507-…-2026-09-25-00-07-10.zip`（37MB，iQOO PD2507）。解包后 `FS/data/misc/bluetooth/logs/` 里只有 **9/16、9/18、9/23 三份旧 `.cfa`**，**没有 9/24 23:55 / 9/25 00:05 这两次刷机时段的日志**；`btsnooz_hci.log` 只有 16 字节（空）。三份旧日志里 ATT(CID 4) 包 **0 个**（是别的蓝牙会话），也不含本项目 DFU 帧。

⇒ 结论：**抓包时段 HCI 日志没开或没落盘**。vivo 的日志文件名是"**蓝牙开启那一刻**"的时间，例如 `BT_HCI_*_UTC+0800.cfa`；日志文件是在**蓝牙关闭时**才写入/收尾的。所以正确顺序必须是：

```
① 开发者选项打开「启用蓝牙 HCI 信息收集日志」
② 蓝牙关 → 开（此刻起算新日志）
③ 用小程序完整刷一次，等到"升级完成"
④ 蓝牙关（关键：这一步才会把日志落盘成 BT_HCI_*.cfa）→ 再开
⑤ 生成 bug 报告（完整报告）→ 把 zip 的 FS/data/misc/bluetooth/logs/ 里最新那份 .cfa 发我
```

**解析器已按 vivo 实际格式修正并验证**：`tools/parse_hci_log.js` 现已支持 btsnoop 标准头（magic8+version4+datalink4=**16 字节**，记录头 24 字节，datalink 1002=HCI H4），能正确解出 ACL→L2CAP→ATT 的写/通知与写入类型。

**当前状态**：序列 ✓、帧长 ✓、写类型 ✓、目标模式 ✓、key ✓、读回 ✗ —— **所有非抓包手段已穷尽**，下一步必须拿到小程序的真实字节（HCI 抓包），才能知道"头部"那一帧的真实字节与命令。

**为定性"是不是帧长"**，已在仿真按钮里加入**传输阶段的帧长上限探测**（在正确序列之后、解锁之前，用只读 `0x8A`+填充测 21/41/61/81/101/102/121/145B）：若该阶段能解析到 ≥102B 而解锁仍静默 ⇒ 帧长不是原因，问题在**解锁帧内容/命令**；若能解析的上限 <102B ⇒ 解锁帧必须拆成多条短帧，抓包目标随即明确。结论文案会自动按这两种情形给出。

**该按钮已按抓包重新校准（2026-09-25，仍不擦除）**：设备信息页 **🔓 照抄小程序：0x84→立刻0x8F→重连→解锁**
`0x8F, 0x8F, 0x8A, 0x85`（识别，可选）→ `0x84`（→`02`）→ **不等断链、在同一条连接上立刻补发 `0x8F`**（对齐抓包 59ms 时序）→ 等 `gattserverdisconnected` → 自动重连并重新订阅 FEE2 → `0x85`（应 `02`）→ `0x8C` → `0x81+头96B`（102B 整帧，应 `02`）→ **到此为止，不发 `0x87`**，只报告"解锁是否被确认"。
- ~~旧写法：`0x8F,0x8F,0x8A,0x85,0x84` → 等断链重连 → 在 DFU 模式下再发 `0x8F`~~ —— 已证实无效（重连后的 `0x8F` 只被当 SN 查询）。
- 同时修掉一个真 bug：`dfuRequest` 会丢弃不在 expect 列表里的应答，而解锁帧的真实应答是 **`02`**，旧 expect `[0x01,0x04,0x0a]` **漏了 02** ⇒ 成功的解锁会被当"错位应答"丢掉。
- mock 回归（`tools/tests/mimic_unlock.test.js`）：mock 设备规则=抓包行为（断链会清掉"已武装"态 ⇒ 旧写法永远切不过去），断言「`0x8F` 在断链前同连接发出」与「`02` 不被丢弃」同时通过。

**已确认可行（官方客户端实测）**
- **官方安卓 APK**（官网 `W66D_APP.zip` 解压所得，md5 `daf75e0f8f0b378e6c74d1d922ea242b`；本仓库未随附，需自行下载）：`0x84 ENTER_DFU` → 设备重启进 DFU → 重连 → `CHECK_IN=02` → `0x8C` 页大小 → 解锁 → 写块。**设备侧该路径我们已复现到"解锁"这一步为止**。
- **微信小程序**（作者 Kai.N，官网致谢"小程序版本：感谢作者 Kai.N"，包加密无法反编译）：**能把同版本 V1.3 完整刷成功**（录屏末帧「升级完成！设备重启中...」100%）。它给出「在线固件 W96D **V1.9**」（官网只挂 V1.3）。
- 小程序刷机时的提示序列（2fps/10fps 逐帧读出，权威）：`发送切换命令 0x8F，等待设备进入传输阶段...` → `设备未自动断开，主动断开旧连接...` → `设备正在切换到升级模式，请稍候...` → `固件 81860 字节，继续传输...` → `FEE2 notify 已订阅` → `确认 OTA 传输阶段...` → `发送 0x8C (DFU 就绪)` → `头部已发送，等待设备确认...` → `设备已接收头部` → `已发送 0x87 (开始数据)，等待设备准备...` → `设备已准备接收数据` → `数据块 N/639`。
- **微信小程序完整刷机的 HCI 抓包（2026-09-25）**：`BT_HCI_*.cfa` —— ✅ **逐字节实证上述全部流程**，结论见 §2.4.0。注意"主动断开旧连接"是**兜底分支**：本次抓包里设备自己超时断的（reason 0x08），小程序并没有主动断开。

**已试遍并排除的维度（全部真机实测）**

| 维度 | 试过的值 | 结果 |
|---|---|---|
| TX key | 0 / 非 0 动态 | 非 0 连小帧都被静默 ⇒ **必须 key=0**（官方 APK 的 `PACKAGE_DEBUG_MODE=true` → `selectTxKey()` 恒返回 0，与网页版一致）|
| 进入升级模式 | `0x84 ENTER_DFU`（APK 路线）/ `0x8F 切换`（小程序路线） | `0x84` 确实让设备重启进 DFU（`CHECK_IN=02`）；`0x8F` 拿到 SN 应答但**设备不重启、重连后仍是应用模式** |
| 目标模式 | DFU 模式 / 应用模式 | 都试过 |
| 写入类型 | 带响应写 Write Request / 无响应写 Write Command | 都试过 |
| 分片方式 | 整帧 / 20B 分片 / 延时重发 | 都试过（含"解锁后立即发 0x87"的时序实验，排除"发得太慢"）|
| 解锁帧内容 | `0x81+头96B`、`0x01+头96B`、`0x82+头96B`、`+总长/体长LE32`、`头32/64/80B`、`0x8F+头96B` | 除对照用的裸 `0x81` 秒回 `01` 外，**所有长帧变体全部静默** |

**抓包后对这张表的最终裁决（2026-09-25）**：除"解锁帧内容"外，其余维度**都不是原因**——抓包里小程序用的正是 `0x81+头96B` 整帧 102B、无响应写、key=0，与我们的字节**逐字节一致**（早前已比对确认）。真正的差异只有一处：**`0x8F` 的发送时机**（抓包是 `0x84` 应答后 59ms 在同一条连接上发；我们是等断链重连之后才发）。详见 §2.4.0 结论 1。

**关键现象（贯穿所有实测）**：6~10B 小帧句句有回（`8A→04 0d`、`8F→0a+SN`、`85→01/02`、`8C→05 00 10`、裸 `81→01`），**102B 帧在任何组合下零应答**（不是回 NG，是彻底不回）。~~⇒ 问题不在"怎么发"，而在设备没把这帧当合法帧解析。~~ ⇒ **2026-09-25 抓包解释**：不是"帧不合法"，而是**切换从未成功**（`0x8F` 发晚了），我们始终在**应用态固件**上发 DFU 数据帧——旧固件对小帧走命令解析、对大帧连收包缓冲都没有，于是静默；传输阶段固件（重连后）对同样的 102B 帧回 `02`。

**~~当前最强假设~~（2026-09-25 已推翻 ⇒ 见 §2.4.0 结论 2）**：W96D 主控是**沁恒 CH592F**，而它的 DFU 服务/特征 UUID 与沁恒官方 BLE OTA profile **完全一致**（`0xFEE0` 服务 / `0xFEE1` 特征；见 [openwch/ch592](https://github.com/openwch/ch592) 的 `OTAprofile.h`）。该 profile 的写缓冲为 `OTAProfileWriteBuf[IAP_LEN]`，老版 IAP 里 **`IAP_LEN = 20`**、发送侧 `OTAProfile_SendData()` 也按 `>20` 分包 ⇒ ~~**每条 BLE 写入可能必须是一整帧且 ≤20B**~~。这能同时解释：小帧正常、102B 整帧被丢、20B 分片被逐片误解析。

> **推翻依据（抓包实测）**：小程序的解锁帧是**单条 102B** 的 ATT 写、数据帧是**单条 134B** 的 ATT 写，全部 `WRITE_CMD`，MTU 协商 247/243。所以 20B 只描述**应用态旧固件**的收包缓冲，与传输阶段无关；WITRN 也没有把整帧拆成短帧发。
（沁恒官方命令集是 `0x80 PROM / 0x81 ERASE / 0x82 VERIFY / 0x83 END / 0x84 INFO`，帧格式 `cmd|len|addr[2]|data`，**与 WITRN 自研命令层不同** ⇒ WITRN 只复用了 profile/传输层。）

**~~尚未验证的一步~~（抓包已给出答案，无需再测）**：帧长边界探测原本用于确认"解锁帧是否必须 ≤20B"。抓包证明解锁帧就是 102B 整帧、数据帧 134B 整帧，**探测已无必要**，相关按钮随 OTA 一并从工具移除。

**下一步（2026-09-25 更新：抓包已完成，只剩"按 §2.4.0 流程重写 OTA"这一件事）**
1. ~~抓官方小程序的真实字节~~ ✅ **已完成** —— `BT_HCI_*.cfa` 解析结论见 §2.4.0；复现命令 `node tools/parse_hci_log.js <cfa>`。
2. **恢复 OTA 实现**（用户点头即可）：按 §2.4.0 的流程写，重点是 `0x84` 应答后 **≤100ms 内同连接补发 `0x8F`**、等断链后重连并**重新订阅 FEE2**，再 `0x85→02` 确认、`0x8C` 读页大小、`0x81+96B` 解锁、`0x87`、639 帧、`0x88`、`0x0B`。
3. 拿到小程序的 `W96D V1.9` 固件（小程序在线固件里，官网未挂）后可作为真实升级目标。

**2.4.0.1 相关开源项目调研（2026-09-24）**

结论先行：**没有任何开源项目实现 WITRN W96D 的 OTA**（该协议是厂商自研）。但有几类项目对后续研究有直接价值：

| 项目 | 类型 | 能提供什么 |
|---|---|---|
| [Halcao/witrn-fan-control](https://github.com/Halcao/witrn-fan-control) | Python CLI/TUI（W96D BLE **控制**） | 唯一针对 W96D 的开源项目；其 `protocol.py` **独立印证**了我们的 GATT 映射（含 DFU 服务 `0xFEE0` / 写 `0xFEE1` / 通知 `0xFEE2`）；README 明确写 **"Firmware DFU ... intentionally out of scope"**，即不含刷机 |
| [openwch/ch592](https://github.com/openwch/ch592)（官方 SDK） | C 源码 + 官方文档 | `EVT/EXAM/BLE/BackupUpgrade_OTA`、`BackupUpgrade_IAP`、**《WCH蓝牙空中升级（BLE OTA）.PDF》**；`OTAprofile.h` 给出官方服务 UUID 与 20B 粒度的 `OTAProfile_SendData` |
| [sintrb/WCHOTA](https://github.com/sintrb/WCHOTA) | **网页版** WCH OTA（`WCHWebOTA.html`） | 同为"浏览器里做 BLE OTA"的参照实现：`mtu=247`、每包 **244B**、全程 **`writeValue`（带响应写）**；阶段间 `delay(500)`；**响应靠 `readValue()` 读回**（不是只等通知） |
| [ZHANGHengEE/WCH_BLE_OTA](https://github.com/ZHANGHengEE/WCH_BLE_OTA) | 网页版 WCH OTA | 命令帧构造 `[cmd,len,addrLo,addrHi,data]`；写入优先 `writeValueWithoutResponse`，可配置包长/包间延时/突发长度 |
| [luckyPtr/wch-ota-tool](https://github.com/luckyPtr/wch-ota-tool) | PC 命令行（Qt + 官方 BLE 库） | `ota.cpp` 实测：`0x80` 编程帧 **240B 数据**、`addr/16` 2 字节、默认 `write(ba, true)` = **带响应写** |
| [xuhongv/WeChatOTAForBlueToothOfPB](https://github.com/xuhongv/WeChatOTAForBlueToothOfPB)、[zhou1591/jlotacustom](https://github.com/zhou1591/jlotacustom) | **微信小程序** OTA 源码（其它芯片） | 小程序侧 BLE OTA 的分包与节奏写法参考（厂商小程序 Kai.N 也是小程序实现） |

**两条可直接利用的新线索**
1. **响应可能要靠 `readValue()` 读回，而不是只等通知**：两个网页版 WCH OTA 工具都是"写命令 → `delay` → **`readValue()`** 取结果"（WCH profile 的 FEE1 是可读可写的）。我们此前只监听 FEE2 通知——若 W96D 的解锁应答走的是"读特征值"，那就会表现为**静默**。恢复 OTA 研究时，第一条就该试：发完解锁帧后 `FEE1.readValue()`。
2. **"浏览器发不出大帧"不成立**：`sintrb/WCHOTA` 在浏览器里稳定发 **244B/包**（`mtu=247`）。所以大帧本身不是 Web Bluetooth 的限制，我们的 102B 静默仍是设备侧行为。

**线索①实测结果：已排除（2026-09-24 23:45 真机）** —— `FEE1` 属性显示 `read=true write=true writeWithoutResponse=true`，但**每一次 `readValue()` 都返回 `GATT operation not permitted`**（基线读、小命令后、解锁帧后、带响应写/无响应写复测、收尾读，全部如此）⇒ 设备虽然**声明**可读，ATT 层却拒绝读（固件侧读权限未开/要求加密链路）。**应答只走 FEE2 通知**这一结论成立，同时也说明：我们对 102B 帧"静默"的观测是真实的，不是漏读。
顺带确认：小命令 `0x8A` 在 164ms 后经通知正常返回 `04 0d`，说明监听通路本身没问题。

**线索②首轮实测（2026-09-24 23:48，应用模式）—— 结果非常有信息量**：

```
[边界] 当前模式 CHECK_IN=01（应用）
[边界] 帧 7B(2B) ✓01  8B(3B) ✓01  9B(4B) ✓01  10B(5B) ✓01  11B(6B) ✓01  12B(7B) ✓01
[边界] 帧 13B(8B) ✗   14B(9B) ✗   15B(10B) ✓01  16B(11B) ✗   17B(12B) ✓01  18B(13B) ✓01
[边界] 帧 19B(14B) ✗  20B(15B) ✗  21B(16B) ✗（连续三次 → 停止）
```

两点关键读数：
1. **带填充的 `0x8A` 帧回的是 `01`（NG），而不是 `04 0d`** ⇒ 设备**确实解析了**这些 13~18B 的帧，只是按"该命令的长度不符"拒绝了它们（裸 `0x8A` 才回 `04 0d`）。说明设备**按命令校验 payload 长度**。
2. **从 19B 起连续无应答** ⇒ **应用模式**下的接收上限约 **18~20B**，与沁恒 OTA profile 的 20B 写缓冲高度吻合（散点的 ✗ 判定为回包时序噪声，故新版测试每档复测 2 次）。

**但有一个必须解释的矛盾**：小程序是 **639 块 × 128B（帧 134B）**刷成功的 ⇒ **传输/DFU 模式下的接收缓冲必然远大于 20B**。因此 18~20B 这条界线**只适用于应用模式**，真正有意义的是 **DFU/传输模式下的上限**——若那里也是 ~20B，则小程序必然把 128B 块拆成多条短帧发（且设备能跨写重组），我们的思路要整体调整；若那里能收 ≥134B，则"帧长"根本不是解锁失败的原因，必须回到抓包。

**关键补充实测（2026-09-24 23:5x，DFU 模式）**：用户复测到 **DFU 模式下 41B 帧仍有应答** ⇒ **"20B 写缓冲"在 DFU 模式不成立**，那条 18~20B 界线是**应用模式特有**的。于是真正要问的是"**DFU 模式的上限在哪**"（我们的解锁帧 102B、数据块帧 134B）。

**为此把帧长边界测试扩到 145B，并改成粗扫 + 细扫**：粗扫帧长 7B→145B（失败即复测一次抗噪声），遇到首个失败点后在其前 14B 范围内以 2B 步长细扫（每档复测 2 次）。判读按两个门槛分档：
- 上限 **≥134B** ⇒ 帧长足够装下 128B 数据块与 102B 解锁帧 ⇒ **帧长不是解锁失败的原因**，必须回到内容/状态（或抓包）；
- **102B ≤ 上限 < 134B** ⇒ 解锁帧能整帧发（我们的做法没错），但数据块帧必须分片；
- 上限 **< 102B** ⇒ 连解锁帧都发不出，必须拆成多条短帧（且设备需能跨写重组）。

**新版帧长边界测试（build v20260924b+）**已支持：① 应用模式下弹窗询问"是否先自动进 DFU 再测"（走 `0x84`，含断链等待+重连+句柄校验，**不擦除**）；② 帧长 7B→41B 逐档；③ **每档复测 2 次**、间隔 700ms、等待 2.5s，以区分真边界与噪声；④ 输出明细表 `帧长:第1次/第2次`。

**线索②（沁恒 20B 写缓冲）已落地为工具按钮（待跑）**：设备信息页 **📏 帧长边界测试** —— 用只读命令 `0x8A` 加填充字节，帧长从 6B 逐字节加到 41B（每帧间隔 500ms，避免上一帧迟到回包被误算），找出"从多少字节开始不再应答"。判读：**边界正好 20B ⇒ 20B 写缓冲假设成立**（解锁帧必须拆成多条 ≤20B 短帧，这能解释全部现象）；**6~41B 全通 ⇒ 假设不成立**，回到抓包。

**（以下为线索①落地时的原始说明）**：设备信息页新增 **🔬 DFU 应答通路测试** —— 对每条命令同时观察【FEE2 通知】与【`FEE1.readValue()` 读回】，并做三档对比（基线读 / 小命令 `0x8A` 后 / 解锁帧 `0x81+头96B` 后，带响应写与无响应写各一次）。判读：解锁帧之后读回值**相对基线或小命令发生变化** ⇒ 设备用"读特征值"返回应答（此前只等通知，漏掉了）；读回始终等于基线 ⇒ 回到"设备确实没解析该帧"的结论。**测试不发 `0x87`，不擦除。**

**须注意**：沁恒官方命令层（`0x80 PROM / 0x81 ERASE / 0x82 VERIFY / 0x83 END / 0x84 INFO`，地址以 `addr/16` 表示、2 字节）**与 WITRN 的 `0x8A/0x8F/0x85/0x8C/0x81/0x87/0x88/0x0B` 完全不同** —— WITRN 只复用了 profile/传输层，**无法直接套用任何 WCH 开源工具刷 W96D**。

**代码状态（2026-09-25）**：`archive/` 目录**已不存在**（用户 09-25 清理无用文件时删除），含完整 OTA 实现的那份 HTML **已不可恢复**；当前 `W96D统一控制台.html` 只保留 DFU 服务的**版本/SN 读取**（设备信息页用），OTA UI 与刷机代码已全部移除。⇒ 若要重做 OTA，请**按 §2.4.0 的流程重写**（协议已完全确定，实现量不大），不要再找旧代码。

#### 2.4.1 固件包格式实测（W96D_V13.up，81860B）

头部 96 字节（0x60），变长 ASCII 字段以 `00` 分隔：

| 偏移 | 长度 | 内容（V1.3 实测值） | 含义 |
|---|---|---|---|
| 0x00 | 32 | `105a109580404617889a6d116bbe7896` | 32 字符 hex 校验串（≠文件/体的 MD5/CRC32，算法未定） |
| 0x21 | 7 | `W96D001` | 型号（工具据此校验适配性） |
| 0x32 | 8 | `00013F64` | **固件体长度**（hex）= 81764 = 文件总长 − 96 ✓ |
| 0x3B | 5+3+8 | `845EC`、`33E`、`000133A8` | 附加 hex 字段（疑似加密参数/入口地址，未定） |
| 0x4D | 1 | `0D`（V1.3）；W96P_V1.8.up 实测 `12`(18)→V1.8 | **版本字节**：十进制拆 major/minor，同 GET_VERSION(0x8A) 编码 |

固件体（自 0x60 起）：**非明文 ARM 镜像**（无 Cortex-M 向量表特征），高频字节 0x43；单字节 XOR 0x43 解码后 0x00 占比 ~10% 但仍无有效向量表 → 压缩或滚动密钥加密，解包不做（工具整体透传）。工具只校验头部（型号/体长）后按 DFU 流程上传原始体。

#### 2.4.1.1 固件包清单与型号匹配（rom/ 目录实测，2026 统一控制台）

| 文件 | 大小 | 格式 | 型号字段 | 版本 | 适用设备 |
|---|---|---|---|---|---|
| `W96D_V13.up` | 81860B | .up（明文头） | `W96D001` | V1.3 | NEW W96D（BLE） |
| `W96P_V1.8.up` | 117696B | .up（明文头） | `W96P`（4 字符） | V1.8 | W96Pro（BLE） |
| `W96D_1.8.df` | 50240B | .df（**加密**） | 不可读 | 取自文件名 | 老版 W96D（HID） |
| `W96D_5.2.df` | 50240B | .df（**加密**） | 不可读 | 取自文件名 | 老版 W96D（HID） |

- **.up**（BLE DFU，统一控制台支持）：头 96B 明文，工具解析型号+体长+版本；刷前校验**设备名前缀 vs 文件型号前 4 字符**（`W96D_000123`→`W96D`，`W96P_555001`→`W96P`；DFU 模式广播名不带 SN 后缀同样命中），交叉刷（Pro 刷 W96D 包或反之）硬拦截。
- **.df**（老版 HID，官方 PC 工具 WITRN_UP 专用，统一控制台**不支持**）：整包加密——高频字节 0x28（12738 次，远超第二名）疑似 XOR 0x28 后的 0x00 填充，但头部解码无有效结构；两个版本二进制差异 31171B 呈雪崩分布（确认整体加密而非局部补丁）；差异段 72..268（ARM 向量表区）逐项异或恒为 0x36、间隔 4 字节。WITRN_UP.dll 引用 RSACryptoServiceProvider/AesCryptoServiceProvider 佐证容器加密。两版本**唯一已知差异是机身「−」键行为**（1.8=逐级降档，5.2=任意档位按住直接停）→ 老版无法软件切换该行为，只能刷不同固件；蓝牙版（NEW W96D/Pro）经 GEAR_DOWN_MODE(fff6) 支持软件切换，此功能 HID 下隐藏（bleCtrl 仅蓝牙显示）。

#### 2.4.2 真机 DFU 应答实测（V1.3，设备 W96D_000123，2026 统一控制台首测）

| 命令（发送 payload） | 设备应答 | 结论 |
|---|---|---|
| `84` ENTER_DFU | `02` OK；**首次可能无应答**（设备直接重启进 DFU，BLE 断开→超时） | 重连后第二次立即成功 |
| `85` CHECK_IN_DFU | `02` OK（DFU 模式）/ `01` NG（应用模式，14:35 实测） | NG=不在 DFU，是分支信号不是错误 |
| `8A` GET_VERSION | `04 0D` | DATA_VERSION=4 + 版本 13（14:24 误判"命令号回显"导致丢弃，14:36 证实回执=04） |
| `8C` GET_PAGE_SIZE | `05 00 10` | DATA_PAGE_SIZE=5 + LE u16 页大小 0x1000=4096B |
| `81` REQ_UNLOCK | `01` | DATA_REQ_UNLOCK=1（**非 NG**） |
| `82` WRITE_FLASH（带 ACK 位 0x82） | `01` = **NG** | 14:36 实测：未发 START_UP 擦除就写 → 被拒。官方写法=**不带 ACK 位**（0x02）、**不回执**，靠 GATT 写成功推进 |
| `88` END_UP（未写数据） | `01` = NG | 演练（无 WRITE_FLASH）时设备拒绝结束——预期行为 |
| `8A` GET_VERSION | `04 0D` | V1.3 ✓ |
| `8F` GET_SN | `0A 52 A2 01 00` | SN=0x01A252=000123，与广播名 `W96D_000123` 一致 ✓ |

其他实测：FFC1 读回 `READY,`（非 BLE_SN=x，SN 状态以设备名后缀为准）；页大小 4096B；~~WRITE_FLASH 需带 ACK 位~~（14:36 证伪：带 ACK 位且未 START_UP 擦除时被回 01=NG，官方写法无 ACK 位无回执）。PC 端官方工具 WITRN_UP_V30（.NET/WPF，C# 混淆）流程含 erase→write→verify，协议族同源（`55` 帧 + FJCHid* 原生 HID 库）。

**14:35–14:36 真机日志完整复盘（一键自动流程首次实测）**：
1. 应用模式连接（`W96D_000123`）：GetVersion `04 0d`✓ → CheckIn `01`NG（正确识别不在 DFU）→ EnterDFU `02`✓。
2. **教训①（已修）**：EnterDFU 后 2s 就 `gatt.connect()`"成功"——设备还没断链，连进的是垂死会话，CheckIn 无应答超时。修法=先等 `gattserverdisconnected`（≤10s）再重连，连上稳 1s，CheckIn 重试 3 次。
3. 14:36:08 设备才真正断链（重启完成）；手动重连后设备已在 DFU：CheckIn `02`✓ → PageSize `05 00 10`✓ → Unlock `01`✓。
4. **教训②（已修，根因）**：WRITE_FLASH 首块（带 ACK 位 0x82、addr=0）→ 设备回 `01`=NG。官方顺序是 **Unlock → START_UP（擦除）→ 300ms → 写入**，我们漏了 START_UP；且写入应无 ACK 位无回执。修法=按官方顺序补 START_UP（15s 超时）、写入改 `dfuWrite`（0x02 无 ACK、写成功+5ms 推进）、END_UP→RESET（0x0B 无应答）收尾。
5. 另发现浏览器可能缓存旧脚本（帧长与源码不符），已在 otaRun 入口打 build 标记便于核对。

**14:47 真机日志复盘（build20260614 首测）**：官方握手全程首次打通——CheckIn NG 正确识别应用模式 → EnterDFU `02` → 等 10s 观察到断链 → 自动重连成功 → CheckIn `02` → PageSize `05 00 10` → Unlock `01` → **START_UP `01`=NG 失败**。
根因（smali pswitch_39 复核）：**REQ_UNLOCK 不是裸命令**——`buildDataPayload(0x1, true, copyOfRange(binData, 0, 0x60))`，即 payload = `0x81 + 固件头 96 字节`（md5 串+型号+体长+版本，作为解锁钥匙）。裸 `0x81` 设备照样回 `01` 回执（无法从应答区分成败），但解锁未生效 → 后续 START_UP 擦除被拒。修法=REQ_UNLOCK 带上 `OTA.file.buf[0..96]`（build20260614-2）。

**14:54 真机日志复盘（build20260614-2）**：带 96B 头的 REQ_UNLOCK（102B 帧）设备**全程静默** 5s 超时；此前裸 `0x81`（7B 帧）秒回 `01`。
排查过程：先用 node 复刻 14:54 帧——用真实 `W96D_V13.up` 头 96B 组帧，算出 CRC=`4e`，与日志末字节**完全一致**，排除组帧/CRC/异或错误；再核对帧长 102 ≤ 197 单包写入无分块问题。唯一与官方 APP 不同的剩余变量：**TX key=0**。
根因（smali `BlePackageProtocol.selectTxKey` + `BleConnectionSession.requestMtuAndSyncWriteChunk`）：官方 APP key=`elapsedRealtime()&0xff` 且 **0→1，从不发 key=0**；官方网页 V3.8 虽用 key=0，但它只发 `8A/8F` 小命令（≤7B 帧），从未发过大帧。~~修法=TX 改非 0 key~~——**此推断 15:37 被真机证伪，见下**。
连带修正两处潜在写坏 flash 的旧账（headless E2E 内容级断言抓出）：
1. **WRITE_FLASH 无地址字段**：smali 复核 `buildWriteChunk` 只产出纯固件数据（0xFF 填充、末块 4 对齐），`getWriteChunkRawSize` 也不含 +5——地址由设备端自增。旧实现 `[0x02, addr32LE, data]` 的 4B 地址会顶掉首 4B 固件数据。改为 `[0x02, ...data]`。
2. **分块切片 +96 重复**：`buf.slice(96+off, 96+min+96)` 尾部多加 96 → 224/96/0 字节交替错位块。改为 `slice(96+off, 96+off+min)`。E2E 现校验：639 块（638×128 + 末块 100）逐字节等于文件体。

**15:37 真机日志复盘（build20260614-3，证伪非 0 key 方案）**：三次 OTA 尝试 + 信息页读取，**所有帧带非 0 key，全部静默**——8a/8f/85 连 7B 小命令都无一应答（`<< [SN显示] 52 45 41 44 59 2c` 是 ffc1 特征读取（"READY,"状态串），与 DFU 无关，别误读）。对照 key=0 时代 8a 秒回 `04 0d`、85/8c/81 全部正常应答，结论反转且铁证：
**W96D 只认 key=0**（与官方网页一致）；W66D APP 的动态 key 方案完全不适用于 W96D——这两个产品的 bootloader 协议在 key 语义上不同。build20260614-4 复原 key=0。
14:54 大帧静默的真正原因重开两条线索（build20260614-4 一并处理）：
1. **大帧传输方式**：14:54 的 102B 单包写入未抛错（说明协商 MTU ≥ 105，帧完整到达）却静默。自适应探测：首个大帧先按 **20B 分片**（仿 MTU23 手机的流式重组路径）发送并等 2.5s，无应答则同帧**改单包**重发；哪种方式先收到应答（哪怕是错位包）就记忆为 `dfuBigMode`，后续大帧直接复用。
2. **同版本保护**：文件 V1.3 == 设备 V1.3（`04 0d`）。官方 APP 明确拒绝相同版本（「版本相同，无需升级」），设备 bootloader 很可能也静默拒绝 ≤ 当前版本的解锁钥匙。现在 GET_VERSION 后若 `文件版本 ≤ 设备版本`，弹窗允许把**解锁头的版本字节（@0x4D）临时 +1**（只影响解锁钥匙，不改变写入内容，重刷后仍为原版本）。

**15:56 真机日志复盘（build20260614-4，传输嫌疑排除）**：key=0 复原后全链路恢复（8a `04 0d`、8f `0a 7b 00 00 00`=SN 000123 ✓、ENTER→断链→重连→CheckIn→PageSize 全绿）。unlock（原版本头）**20B 分片 2.5s 无应答 → 同帧单包重发也无应答**：单包 102B 写入未抛错 = MTU ≥ 105、帧完整到达；分片路径同样无声。**两种传输方式都静默 = 帧确定到达，是设备自己在"决定不回话"**——传输层嫌疑正式排除。剩两个解释：
1. **同版本静默拒绝**（用户本轮选了"用原版本"，还未测 +1 版本字节）。
2. **W96D 解锁成功不回包**：smali 复核官方期望 unlock 成功应答=`02`（`dfuHandleCtrlOk` → `dfuOnPendingOk(REQ_UNLOCK)` → 转 START_UP），裸 `01` 走 `dfuHandleCtrlNg`=重试 3 次=失败。W96D 对 unlock 回 `01`（14:47 裸 0x81）甚至静默（15:56 带全钥匙），与 W66D 语义不同——完全可能它解锁成功就是不出声。

build20260614-5 的应对：REQ_UNLOCK expect 同时收 `01/02`；**超时不放弃，直接探测 START_UP**——未解锁时 START_UP 必回 NG（14:47 已证），是安全闸门，不会误擦：探测 `02` = 解锁成功不回包，继续写入；探测 `01`/超时 = 设备确实没接受钥匙，报错终止。

**16:10 真机日志复盘（build20260614-5）+ 小程序线索**：START_UP 探测回 `01`=NG ⇒ **"解锁成功不回包"假设排除，解锁确实没生效**（用户本轮仍选"用原版本"，同版本假设未测）。
转折点：用户找到**官方微信小程序能刷同版本 V1.3**（作者 Kai.N，圆形艺术码，标准 QR 解码器无法解）。这条实证推翻"设备拒绝同版本"这一解释——文件与协议本身是可行的，差别只可能在**帧的送达方式**。

**关键新线索：Write Request vs Write Command（此前从未测过）**。两个能刷机的客户端都用「带响应写 Write Request」：
- 官方安卓 APP：fastble 的 `BleConnector.writeCharacteristic` 只调 `setValue`+`gatt.writeCharacteristic`，**全库没有 `setWriteType`**（已全量 baksmali 搜索确认）⇒ 走 Android 默认 `WRITE_TYPE_DEFAULT` = 带响应写。
- 微信小程序：`wx.writeBLECharacteristicValue` 有 `writeType` 参数（[官方文档](https://developers.weixin.qq.com/miniprogram/dev/api/device/bluetooth-ble/wx.writeBLECharacteristicValue.html)：`write`=强制回复写 / `writeNoResponse`=强制无回复写），社区实践普遍是**每次≤20B、按 success 回调推进**——带响应语义。
- 本工具历史实现只用过 `writeValueWithoutResponse`（Write Command）。

而实测事实恰好落在"Write Command 大帧不可用"上：≤10B 帧全部秒回，102B 帧（分片与单包两种发法）全静默、连 NG 都不回 ⇒ 帧在设备解析层被丢弃，而非解锁逻辑拒绝。

**build20260614-6 改动**：
1. 大帧写入方式按候选轮换探测：**带响应写×20B → 带响应写×整帧 → 无响应写×20B → 无响应写×整帧**，每种等 2.5s（写抛错立即换），首个收到应答的组合记入 `S.ble.dfuBigMode`，后续大帧（含 WRITE_FLASH 的 134B 帧）直接复用。
2. 新增 **🔍DFU 诊断**按钮（设备信息页「固件更新」卡片）：打印 FEE1/FEE2 特征属性 → 裸 0x81 控制组 → 对 102B/70B/38B 三种帧长 × 4 种写入方式逐一探测（**不擦除、不写入**），用于精确定位"设备接受的写入方式 + 帧长上限"。

补充记录：固件头 `[0..32)` 的 32 字符 hex 串**不是** body/全文件/头 96B 的 md5（三者均已算，全不匹配）——可能是加密前明文的摘要或包标识，暂列为未解项，不影响刷机流程。

**16:49 首版 DFU 诊断日志（build20260614-6）**：`write=true, writeWithoutResponse=true`（带响应/无响应两种写都可用）；`notify=true, indicate=false`（FEE2 用 notify）。会话在**应用模式**下进行（风扇服务读值正常）。结果：
- 控制组 裸 0x81（6B/无响应写）→ ✓`01`（秒回，管道正常）；
- **102B**（0x81+头96）四种写入方式**全部无应答**；
- **70B**（0x81+头64）带响应写×20B → ✓`01`；
- **38B**（0x81+头32）四种写入方式**全部无应答**。

两点重要判读：
1. **写入方式不是 102B 静默的原因**：带响应写（`req-20`/`req-single`）同样全静默 ⇒ 16:10 提出的"Write Request 才是正解"假设对 102B 帧**不成立**（但 `req-20` 在 70B 上拿到了唯一的长帧应答）。
2. **首版诊断有归因瑕疵**：探针在窗口结束后才打日志，`102B cmd-20 无应答(16:49:19)` 与 `70B req-20 ✓(16:49:19)` 落在同一秒 ⇒ 70B 那次"立即应答"很可能是**上一帧 102B 迟到回包**被新探针的监听器捕获。因此"6B✓/38B✗/70B✓/102B✗ 交替"这一模式**不能确认**：可能是 ①38B/102B 真静默而 70B 有效；②102B 有应答但延迟 >2.5s，而 38B/70B 其实都静默。

**build20260614-7（诊断 v2）**用于消歧，全部只读不动 flash：
1. 每个探针记录**精确回包延时**（从"写完成"起算），迟到回包不再被误计给下一帧；
2. 每档**前后各插一次「裸 0x81 控制探针」**——若某档之后连控制探针也不回，说明设备解析器被卡死（而非帧被拒）；
3. 等待时间拉长到 6s（覆盖慢回包）；
4. 帧长阶梯扩到 8 档：头 16/32/33/48/64/65/80/96 字节（帧 22/38/39/54/70/71/86/102B），精确定位设备的 payload 上限或有效区间；
5. 102B 再按三种写入方式复核一次（确定性 + 方式无关性），并用**版本号 +1 的头**再测一次（同版本闸门假设）；
6. 开场用 CHECK_IN 判定当前是 **DFU/bootloader 还是应用模式**（bootloader 行为可能不同，建议在 DFU 模式下跑）。

**16:56 录屏分析：真机刷机成功的 APP 不是我们手上的 APK**（用户提供 40s 手机录屏，逐帧核对）：

- 录屏里的界面（标题「固件升级」/卡片「OTA 固件升级」，右上角是微信小程序胶囊按钮）**把同版本 V1.3 刷成功了**：末帧显示「升级完成！设备重启中...」进度 **100%**，`已连接 W96D [DFU]`，SN 000123，固件版本 V1.3，`已选择: W96D_V13.up`（手动选的同版本文件）。⇒ **"设备拒绝同版本"假设彻底排除**，问题只在发帧方式。
- 该小程序有**在线固件服务**：`在线固件 可升级，当前 V1.3 → W96D V1.9`，附更新说明（标准/节能模式、库仑计 0 转清零放宽、睡眠灯光控制与自动关闭、低压保护、配合微信小程序加入软件库仑计功能）。**官网只挂到 V1.3（W96DN_V1.3.zip），V1.9 只在这个新 APP 的在线固件里**（按命名规律盲试 `W96DN_V1.9.zip` 等 6 个 URL 全 404）。
- 与我们手上 APK 的比对（用于排除法）：官网 `W66D_APP.zip` 下载解包后的 APK **md5 与本地完全一致**（`daf75e0f...`），而逐条解码该 APK 全部字符串后确认它**不含**录屏 UI 的任何文字（"在线/联网/最新固件/搜索并连接/设备处于" 命中 0 次）⇒ **录屏里的是另一个更新的原生 APP**，不在官网页面上分发。

**17:20 录屏再分析：它是微信小程序，而且状态提示把整条流程写在了脸上**（同一段 40s 录屏，按秒裁出状态提示条后逐条读出）：

小程序（作者 Kai.N，包加密无法反编译）刷机时的提示序列（原文，按时间顺序）：

```
已连接 W96D  W96D_000123 ／ SN 000123 ／ 固件版本 未读取 → V1.3
① 发送切换命令 0x8F，等待设备进入传输阶段...      （×4 次刷新）
② 设备未自动断开，主动断开旧连接...               （×3）
③ 设备正在切换到升级模式，请稍候...               （×3）
④ 固件 81860 字节，继续传输...
⑤ FEE2 notify 已订阅                              ← 重连后必须重新订阅通知！
⑥ 确认 OTA 传输阶段...                            ← 10fps 慢放才发现（2fps 漏掉）
⑦ 发送 0x8C (DFU 就绪)                            ← 同上；0x8C 是「DFU 就绪」握手
⑧ 头部已发送，等待设备确认...
⑨ 设备已接收头部                                  ← 解锁头被 ACK
⑩ 已发送 0x87 (开始数据)，等待设备准备...         （×3）
⑪ 设备已准备接收数据
⑫ 数据块 25/639 → 50 → 80 → 105 → 135 → 160 → 190 → 215 …（进度 40% 时 120/639）
```

（⑤–⑦ 是 10fps 抽帧 + 二值化像素差去重后补出的——2fps 抽帧会把这几条一闪而过的提示漏掉。**⑥⑦ 正是用户观察到的那步**：重连后先"确认 OTA 传输阶段"，再发 `0x8C` 作为"DFU 就绪"握手，然后才发头部。）

**这张提示表直接给出了正确流程，纠正了我们此前全部猜测**：

| 环节 | 小程序实际做法 | 我们此前做法（错） |
|---|---|---|
| 进传输/升级模式 | **发送 0x8F「切换命令」**并短暂等待 | 发 `0x84 ENTER_DFU` 等设备重启断链 |
| 断链处理 | **设备不自动断开就主动断开旧连接**（明确写在提示里） | 干等设备自己断（≤10s） |
| 重连后 | **订阅 FEE2 notify → 确认 OTA 传输阶段 → 发 `0x8C`（DFU 就绪，兼取页大小）→ 发解锁头** | 先 `0x85` CheckIn 再 `0x8C` PageSize，且未显式重订阅 |
| 解锁 | 发 `0x81+头96B` →「头部已发送，等待设备确认...」→「设备已接收头部」（ACK） | 同样发，但在错误的阶段，设备静默 |
| 开始数据 | 发 **`0x87`（START_UP）** → 等「设备已准备接收数据」 | 解锁后才发 |
| 写块 | **639 块**（128B/块，与我们一致：81764B÷128≈638+末块） | 一致 |
| 固件长度 | 提示显示 **81860 字节**（整个文件长度，非体长 81764） | 我们按体长 |

⇒ 结论：**0x8F 才是 W96D 进入传输阶段的开关**（用户此前的直觉完全正确），我们那条 `0x84→DFU` 通路不是小程序走的通路——这也解释了为什么设备在 0x84 之后对 102B 解锁帧静默。

**build20260614-10 → -12 改动：整体照抄小程序流程**（`otaHandshakeUnlock` 重写）：
应用模式读 `0x8A` 版本 → 发 **`0x8F` 切换**（等 ≤3s，不强制要应答）→ 若未断链则**主动 `gatt.disconnect()`** → 重连（≤20×1.2s，`bleDiscover` 内自动 `DFU_N.startNotifications()`，对应小程序「FEE2 notify 已订阅」）→ 稳 800ms → **确认 OTA 传输阶段**（`0x85` CHECK_IN，-12 补）→ **发 `0x8C`（DFU 就绪，-12 按小程序原文；兼取页大小，失败按 4096B）** → 发 **`0x81+头96B` 解锁**（期望 `01`=「设备已接收头部」，`expect` 覆盖为 `[01,04,0A]` 以免把 ACK 当 NG）→ 发 **`0x87`** 等就绪 → 639×128B 写块 → `0x88 END_UP` → `0x0B RESET`。日志文案与小程序提示一一对应，便于逐行对照。E2E（mock）已验证全序列：`8a→8f→(断开重连+订阅)→8c→81(97B)→87→639 块→0b`。

**22:59 真机日志复盘（build20260614-12，抓到两个真 bug）**：

1. **重连后会话是坏的**：`确认 OTA 传输阶段：CHECK_IN 无应答（DFU 服务不可用）` / `0x8C 无应答（DFU 服务不可用）` / `解锁头无应答（DFU 服务不可用）`。根因：`bleDiscover` 里 `getPrimaryService`/`getCharacteristic` 的异常被 `catch` 吞掉，于是"GATT 连上了但 DFU 句柄没取到"，后续每条命令都在 `charOf('DFU_W')` 处直接抛「DFU 服务不可用」——整轮白跑。
2. **第二轮的切换命令压根没发出去**：`切换命令无应答（DFU 服务不可用）`。所以"重连后 CHECK_IN 仍回 `01`（应用模式）"**不能证明 0x8F 切换无效**——那一轮 `0x8F` 没被发送。
3. 另一条观察（待验证）：第一轮的 `0x8F` 用**无响应写**发出，拿到了 SN 应答（`0a 7b 00 00 00`），但设备**没有断链、也没进传输阶段**。微信小程序的 `writeBLECharacteristicValue` 默认走**带响应写**——很可能切换动作只在带响应写路径上被设备执行。

**build20260614-14 修复**：
- `dfuSendFrame` 支持对小帧显式指定写类型；`dfuRequest`/`otaDfuCmd` 增加 `forceMode` 参数透传。
- **切换命令 0x8F 先按「带响应写×整帧」发，无应答再退回「无响应写」**，日志标注实际生效的写类型。
- `bleDiscover` 末尾**校验 `DFU_W`/`DFU_N` 是否取到**，缺失则抛错；重连循环据此继续重试（不再把坏会话当成功），并打印 `重连服务: …｜DFU_W=true DFU_N=true`。

**23:03 之后的推论修正 + build20260614-16（回到官方 APK 路线并补齐关键组合）**

用户提问「这个从 W66D 官方 OTA 里找不到吗」引出一个重要事实核对：**官方 APK 进入升级模式用的是 `0x84 ENTER_DFU`，不是 `0x8F`**（APK 里 `0x8F` 只是 `CTRL_GET_SN` 读序列号）。而 `0x84` 这条路我们早期**验证过确实生效**：14:36/15:56 日志里 `84 → 02` → 设备断链重启 → 重连后 `CHECK_IN → 02`（已在 DFU）、`0x8C → 05 00 10`。

那么两条已知可行客户端（官方 APK、微信小程序）分别走不同入口（0x84 / 0x8F），却都成功——说明**入口不是关键，发帧方式才是**。核对历史日志后发现一个**从未测过的组合**：

| 场景 | 大帧写入方式 | 结果 |
|---|---|---|
| DFU 模式（0x84 路径）14:54 / 15:56 / 16:10 | **仅无响应写**（Write Command） | 102B 解锁帧静默 |
| 应用模式 16:49 诊断 / 23:03（0x8F 失败后） | 四种方式全试 | 静默 |
| **DFU 模式 + 带响应写（Write Request）** | **从未测过** | **？** |

而官方 APK 的写入是**全带响应写**（fastble 全库无 `setWriteType` → Android 默认 `WRITE_TYPE_DEFAULT`）。⇒ build20260614-16 新增「进入升级模式方式」下拉框，可切回**官方 APK 的 `0x84` 流程**，并让解锁/写块都走 `dfuRequest` 的「带响应写优先」探测——**这正是 APK 的组合，此前从未跑过**。

APK 分支实现：`0x85 CHECK_IN`（02 则已在 DFU）→ 否则 `0x84 ENTER_DFU` → 等断链 ≤10s → 重连（含 `DFU_W/DFU_N` 句柄校验）→ 重连后 `CHECK_IN` 确认 3 次 → `0x8C` 页大小 → `0x81+头96B` 解锁（带响应写探测，超时 8s）→ 交回写入阶段发 `0x87 START_UP` → 639 块 → `0x88` → `0x0B`。

**23:10 真机日志（build20260614-16，官方 APK 路线）——传输层彻底排除**：`0x84 ENTER_DFU → 02` → 断链重连 → `重连后 CHECK_IN=02`（已在 DFU）→ `0x8C → 05 00 10` → 解锁帧 102B 按 **带响应写×20B → 带响应写×整帧 → 无响应写×20B → 无响应写×整帧** 四种方式全试 → **全部静默** → `0x87 → 01` NG。

⇒ **写入方式/分片/写类型/入口模式（0x84 与 0x8F）四个维度全部试遍**，102B 解锁帧在任何组合下都得不到应答；而 6 字节小帧句句有回。对照：**裸 `0x81`（6B）秒回 `01`，加上 96B 头就彻底沉默** ⇒ 设备没有把这条长帧当作合法帧解析，问题在**帧内容/形式的假设本身**，不在传输。

**build20260614-17 新增「🔓 解锁帧探测」**（DFU 模式下逐一试变体，只判"是否有应答"，**不发送 0x87 不擦除**）：
裸 `0x81`（对照）｜`0x81+头96B`（基线）｜`0x01+头96B`（无 ACK 位）｜`0x82+头96B`（写数据位）｜`0x81+头96B+总长LE32`｜`0x81+头96B+体长LE32`｜`0x81+头32B`｜`0x81+头64B`｜`0x81+头80B`｜`0x81+头96B`（无响应写）｜`0x8F+头96B`。
判读：若基线与其它变体都静默 ⇒ 只能靠抓小程序的真实字节；某变体有应答 ⇒ 即解锁帧正确形式。

**23:1x 芯片线索（用户提供）：W96D 用的是沁恒 CH592F ⇒ 查沁恒官方 BLE OTA 方案**（来源：[openwch/ch592 SDK](https://github.com/openwch/ch592)、官方文档《WCH蓝牙空中升级（BLE OTA）.PDF》在 `EVT/EXAM/BLE/` 下）：

- 沁恒官方 OTA 的**服务/特征 UUID = `0xFEE0` / `0xFEE1`**（`EVT/EXAM/BLE/BackupUpgrade_OTA/Profile/include/OTAprofile.h`：`OTAPROFILE_SERV_UUID 0xFEE0`、`OTAPROFILE_CHAR_UUID 0xFEE1`）——**与风扇 DFU 服务（FEE0 + fee1 写 / fee2 通知）完全一致** ⇒ WITRN 直接沿用了沁恒这套 OTA profile。
- 官方 profile 的写缓冲是 **`OTAProfileWriteBuf[IAP_LEN]`，老版 IAP 里 `#define IAP_LEN 20`**；发通知也是 `OTAProfile_SendData()` 里 `if(send_len > 20)` 自行分包 ⇒ **20 字节是这套 profile 的天然粒度**。
- 官方命令集（`ota.h`）：`CMD_IAP_PROM 0x80`（cmd|len|addr[2]|data）、`CMD_IAP_ERASE 0x81`、`CMD_IAP_VERIFY 0x82`、`CMD_IAP_END 0x83`、`CMD_IAP_INFO 0x84`；地址 2 字节小端、帧长 `IAP_LEN`（新版例程 247）。**与 WITRN 用的命令值不同**（WITRN 的 0x8A/0x8F/0x85/0x8C/0x81/0x87/0x88/0x0B 是自研命令层），说明 WITRN 只复用了沁恒的 **profile/传输层**，命令层是自己的。

**由此得到的关键假设**：设备侧写缓冲若照搬 20B，则**每条写入必须是一整帧且帧长 ≤20B**——这与实测完全吻合：6B 帧句句有回、102B 帧（整帧写 与 20B 分片写）全部静默（分片写时第一片被读成 `55 00 61 …` 却等不到 97B 载荷，后续片没有帧头，全被丢弃）。
⇒ **W66D APK 那套 97B 解锁帧在 W96D 上可能根本发不出去**，小程序必然是把"头部"拆成多条 ≤20B 的短帧发的。

**build20260614-19**：在「🔓 解锁帧探测」里加了**阶段 1：帧长边界探测**——用只读命令 `0x8A` + 填充字节，帧长从 6B 逐字节加到 25B，看从多少字节开始不再应答。若边界正好是 **20B**，沁恒 20 字节写缓冲假设即被证实，下一步就该去找"头部拆成短帧"的具体布局。

风险：`0x87` 会擦除 flash。解锁被 ACK 时按小程序流程直接发；**解锁无应答时才弹确认框**（未解锁会被设备 NG 拒绝，不会误擦）。万一中途失败导致风扇停在升级模式，可用微信小程序重刷恢复。

#### 2.4.3 官方安卓 APP（W66D 体验版 APK，baksmali 反汇编）DFU 状态机——2026 统一控制台对齐依据

命令常量（`BleDfuProtocol`）：控制类 CTRL_ENTER_DFU=4 / CHECK_IN_DFU=5 / START_UP=7 / END_UP=8 / RESET=0xB / GET_PAGE_SIZE=0xC / GET_VERSION=0xA / GET_SN=0xF，ACK 位=0x80，CTRL_OK=2 / CTRL_NG=1；数据类 DATA_WRITE_FLASH=2 / DATA_REQ_UNLOCK=1 / DATA_PAGE_SIZE=5 / DATA_VERSION=4 / DATA_SN=0xA。**应答回执=对应 DATA_*/CTRL 值**（GET_VERSION→`04+版本`、GET_PAGE_SIZE→`05+数据`、GET_SN→`0A+SN`、控制命令→`02`OK/`01`NG——14:24 真机日志证实）。**REQ_UNLOCK 的成功应答按官方路由=裸 `02`**（`dfuHandleCtrlOk`→`dfuOnPendingOk(REQ_UNLOCK)`→转 START_UP）；裸 `01` 走 `dfuHandleCtrlNg`=重试 3 次=失败——W66D 语义如此；W96D 真机实测对 unlock 回 `01`（14:47 裸命令）或静默（15:56 全钥匙），语义不同，统一控制台 expect 同时收 `01/02`。网页端首版曾把回执当"命令号回显"导致 GET_VERSION 的 `04 0d` 被当错位包丢弃。

状态机（`MainActivity.dfuStart` 起步）：

1. **GetVersion**（文件版本标记 @0x4D 需 10–99，产品名 @0x21）。GetVersion=0 → 无APP，继续 CheckIn；=文件版本 → 「版本相同，无需升级」终止；不同 → CheckIn。
2. **CheckInDFU** → NG = 不在 DFU → **EnterDFU**（最多重试 3 次，超时也算）→「EnterDFU OK/未收到 OK，统一进入同一套重连流程」：等设备**主动断开**（6s watchdog 兜底）→ **自动重连扫描**（快速重试 1 次 @300ms + 最多 3 次 @1s + 扫描超时 20s）→ 重连成功 → **CheckInDFU 确认** → GetPageSize。
3. **GetPageSize**（1s 超时，重试 3）→ `findBestChunkSize`：从 0xFC 向下按 4 递减找第一个满足 `pageSize % chunk == 0` 的值 → **ReqUnLock**。
4. **StartUP 在写入之前**！ReqUnLock OK →「解锁成功，发送 StartUP」（进度文案"擦除Flash"）→ StartUP OK → 延时 300ms → **WriteFlash** 循环：`buildWriteChunk(binData, 0x60, offset, chunkSize)`——**固件体自 0x60 起，纯数据无地址字段（设备端自增地址）**，仅最后一块 4 对齐 0xFF 填充；数据命令 `buildDataPayload(2, false, chunk)`——**WRITE_FLASH 不带 ACK 位、不带地址**；成功推进靠**写入回调 onSuccess**（非设备逐块回执！），块间 `postDelayed 5ms`；offset≥len-0x60 → **EndUP**（12s 超时）→ OK →「发送 Reset」→ RESET 命令（无 ACK 位，onSuccess 即完成）。GetPageSize 与 ReqUnLock 之间无其他命令；ReqUnLock 的应答=裸 `01`（payload 长=1 时按 CTRL_NG 分派，但 GetVersion/CheckIn/PageSize 之外的 pending 收 `01` 走 `dfuOnPendingOk`=成功语义——官方用"pending 阶段"消歧）。
5. 过程中 NG：WRITE_FLASH 中 NG=失败；其他 pending NG=重试 3 次后失败。断连：WRITE_FLASH/其他阶段=失败，仅 WAIT_REBOOT/SCAN_RECONNECT/CONNECT_RECONNECT 阶段断连=走重连。
6. 附加：设备 SN 查询 `buildControlPayload(0xF, true)`，应答 DATA_SN=0xA + LE32；写入帧 timeout=3s，普通命令 1s。

帧层（`BlePackageProtocol`）：`55 | key | len(LE16) | payload | CRC8`；key 由 `selectTxKey()` 生成，而 **`debugMode==true` 时直接 `return 0`**（否则 `elapsedRealtime()&0xff`，0 映射 1）；关键：`BleConstants.PACKAGE_DEBUG_MODE = true`，且 `BleConnectionSession` 用无参构造 `BlePackageProtocol()` → 内部 `this(true)` ⇒ **官方 APK 实际每帧都发 key=0**（动态 key 分支在此 APK 中不会走到）。这与官方网页（key=0）、W96D 真机实测（非 0 key 全静默／key=0 全部应答）三方一致——**统一控制台固定 key=0 正确**（此前"官方从不发 0"的结论已作废）。body(≥2 起)与 len 字段 XOR `CRC8_TABLE[key]`；CRC 覆盖 55…payload（init 0x89，表与网页版一致）；payload 合法长度 1..300。传输层：APP 请求 MTU=200（`setMtu(0xc8)`）、`setSplitWriteNum(197)`（仅 >197B 才走 SplitWriter 分片）；fastble 的 `writeCharacteristic` 校验 characteristic 属性含 WRITE/WRITE_NO_RESPONSE（`properties & 0xc`）后调 `gatt.writeCharacteristic(characteristic)`，**全库无 `setWriteType`**（已全量 baksmali 搜索确认）⇒ Android 默认 `WRITE_TYPE_DEFAULT` = **带响应写（Write Request）**——这是与网页端 Write Command 的关键差异，见 2.4.2 的 16:10 复盘；MTU 就绪前通知开启延迟 360ms。

APK 是**产品无关的通用刷机工具**（全代码无 "W66D/W96D" 硬编码）：`startScan()` 要求**先选固件文件**，并用文件头的产品名过滤设备——扫描匹配 `matchesUpgradeScanTarget`：解析广播 AD Type `0x16`(Service Data) 内的结构单元，**单元 `0x2a` 必须等于 `"WITRN"`、单元 `0x26` 必须等于文件头 `[0x21,0x21+0x11)` 的产品名**（本包 = `W96D001`）。固件校验三项：`长度>0x60`、产品名非空（`[0x21..)`，最长 17B，NUL 截断）、版本标记 `[0x4D] ∈ [10,99]`。**APK 自身拒绝同版本**：设备版本 == 文件标记 →「版本相同(x.x)，无需升级」直接终止（所以用 APK 重刷同版本必须把 `[0x4D]` +1 生成测试包；该测试包可由一条 node 命令再生，见 README）。

与统一控制台旧实现的差异（真刷卡解锁根因）：旧版无条件先发 ENTER_DFU——应用模式下设备回 02 后即重启断链，后续 CHECK/PAGE/UNLOCK 的应答**逐条迟到错位**（0x8c 收到 02、0x81 收到 05 00 10，假成功后 WRITE_FLASH 超时）。现已对齐官方：**GetVersion→CheckIn 探测，NG 才 EnterDFU→等断链→自动重连→CheckIn→…**；且 dfuRequest 增加 expect 回执严格配对（数据命令=DATA_* 号、控制命令=02/01），迟到错位应答直接丢弃。

UI 流程（2026 定稿）：设备信息页「固件更新」卡=**选固件(.up) + 一键按钮**——应用模式连接下点按钮即自动执行全流程（CheckIn 探测→NG 则 EnterDFU→设备重启断链→同一 device 对象自动重连（DFU 广播名不带 SN）→CheckIn 确认→握手→写入→完成），无需二次选文件/二次连接；自动重连失败时兜底提示手动连接后点「开始升级」续跑。

---

## 三、反汇编方法记录（可复现）

1. **网页版**：直接抓取 HTML，JS 未混淆，CONFIG + 各函数即协议文档。
2. **安卓版 Flutter（推荐路径）**：
   - APK 解包取 `lib/arm64-v8a/libapp.so` + `libflutter.so`
   - [Blutter](https://github.com/worawit/blutter) 反编译：Dart 3.8.1，输出带符号 ARM64 反汇编 + 对象池（字符串/常量全可读）
   - 本环境适配点：blutter 只支持 ARM64 分析器；`extract_dart_info.py` 需补 PE 头支持（Windows libflutter.dll）；编译需 GCC 13+（`<format>`）
3. **Windows exe**：`data/app.so` 是 x64 ELF 快照，Blutter 不支持 x64 分析，但：
   - 用 `strings` + 对象池规律可直接提取全部命令字符串（`GEAR=0,`、`WIND_*` 等）
   - 与 ARM64 版同源码（`package:w_fan_a/main.dart`），协议可直接沿用安卓版结论
4. **官方安卓 APP（原生）**：apktool 反编译 smali，读 `BleDfuProtocol`/`BlePackageProtocol`/`B/e.smali`（USB 实现）即得完整协议。

---

## 四、统一工具方案

### 4.1 架构建议

```
┌────────────────────────────────────────────┐
│              统一控制台 (建议 Web/Tauri)      │
│  档位/转速/定时/灯光/Turbo │ 自然风曲线编辑 │ 快充协议 │ OTA │
└──────────────┬─────────────────────────────┘
               │  传输抽象层 (Transport)
   ┌───────────┼─────────────┬──────────────┐
   │ BleTransport (Web BLE)  │ UsbTransport │
   │ FFF0/FFD0/FFC0/FFE0/FEE0│ HID 或 bulk  │
   └───────────┴─────────────┴──────────────┘
               │  协议层 (Protocol)
   ┌───────────┼──────────────┐
   │ W96DBleProtocol          │ W96DUsbProtocol(ASCII) │ W96PChargerProtocol │ DfuProtocol(0x55帧)
```

- **传输层**：`write(bytes)` / `onData` 事件。BLE 用 `writeValueWithoutResponse` + notify；USB 安卓用 bulkTransfer，PC 用 hidapi/WinUSB（VID 0x0E2C / PID 0xA206）
- **协议层**：三种设备各一个 Protocol 类；命令构造都是 ASCII `K=V,`（BLE 的 hex 特征写入除外）
- **DFU 模块**：`0x55` 帧协议打包器 + CRC8(0x3B, 0x89)，BLE/USB 通用

### 4.2 技术选型

| 方案 | 优点 | 缺点 |
|---|---|---|
| **Web (PWA) + Web Bluetooth + WebHID** ★推荐 | 与官方 HTML 版同栈、跨平台、零安装；桌面 Chrome/Edge 全功能 | iOS 不支持 Web Bluetooth；安卓浏览器无法直连老版（HID 受保护类） |
| Flutter 单一代码库 | 与原版同栈，hid_android/bt 双通道现成 | 需要自己维护插件 |
| Tauri/Electron + node-ble + usb | 桌面体验好 | 打包体积、权限 |
| **安卓 APK（借鉴官方 neww96d.apk）**（待做） | 安卓上直连老版的唯一途径=原生 USB Host API（官方 APK 即此方案：UsbManager + interrupt 端点收发）；BLE 部分可复用 FastBLE 栈 | 需维护原生工程；桌面上无必要 |

### 4.3 安卓兼容（后续规划）

现状：老版 W96D 是 HID 类设备，浏览器 WebUSB 受"受保护类"限制全平台禁用，安卓又无 WebHID → 手机浏览器无法直连老版（NEW 版蓝牙不受影响）。若需要在安卓上用老版，可参考官方 `neww96d.apk`（apktool 反编译已确认其结构）：
- 包名 `com.clj.blesample.witrn.fan`，FastBLE 库 + 原生 BLE/USB Host 混合
- 老版 USB 通道即 Android `UsbManager` 直接 claim HID 接口后对 interrupt 端点做批量收发（不受浏览器保护类限制）
- DFU 固定走 FEE0/FFE1 写 + FFE2 通知（`BleConstants`：TARGET_SERVICE=FEE0，WRITE=FFE1，NOTIFY=FFE2；MTU 请求 200；写块 0xC5=197B；0x55 帧头，包上限 0x12C=300B，RX 超时 500ms）——与现有统一控制台的 DFU 实现一致，可互相印证
- 控制特征常量 FFF1-FFF7 与本工具 BLE 实现一致

### 4.4 功能清单

1. **设备识别**：BLE 扫 `W96D` 前缀；USB 扫 `0x0E2C:0xA206`
2. **老版功能**：档位/Gear PWM（GSET1-4 对应 UI）、自然风曲线编辑器（组×点×周期→ WIND_IDX/WIND_SUM/WIND_TIM/WIND_SAVE）、遥测仪表（解析 vbus/vbat/ibat/pwm/gear JSON）
3. **新版功能**：转速/定时/Turbo/灯光/休眠/减档模式/档位标定/电池容量/自然风 128 点曲线/SN 与版本读取（DFU 0x8A/0x8F）
4. **Pro 快充**：POW_C_IN/POW_1A/POW_1C/POW_1D 位域开关 + 实时 VBUS
5. **OTA**：`0x55` 帧协议 + 官方固件包（W96DN_V1.3.zip / W96P_V1.8.zip / W96D_V5.2.zip 均为公开下载）
6. **风险提示**：W96P 快充魔改官方已声明高风险（烧设备概不负责）；OTA 中断有变砖风险，建议工具内加双重确认。

---

## 五、交付物清单

| 文件 | 说明 |
|---|---|
| `协议文档_W96D_BLE.md` | New W96D / W96Pro / 快充控制器 BLE 完整协议 |
| `blutter_out_w96d_apk_arm64/` | 老版安卓应用完整反汇编（带符号） |
| `W96D_V3.8.html` / `W96P_charger.html` | 官方网页源码存档 |
| `w96d.apk` / `neww96d.apk` | 安卓版样本 |
| `w96dn_fw/` | 官方固件 V1.3（OTA 格式样本：头部含版本号 ASCII + 型号 `W96D001`） |
