# WITRN 风扇协议与技术文档

覆盖 **老版 W96D（USB/HID）**、**NEW W96D（BLE）**、**W96D Pro（BLE + 快充）** 三个硬件版本：
协议、GATT 映射、DFU/OTA 流程与工具实现要点。配套件：[`README.md`](README.md)、工具 `W96D统一控制台.html`、回归测试 `tools/tests/`；抓包关键数据见 **附录 A**（设备地址/SN 已隐去）。

数据来源：官方网页 `W96D_V3.8.html` / `W96P 快充任意流控制器.html`（JS 未混淆，逐函数核对）、官方安卓 APK `W66D_APP.zip`（baksmali 反汇编）、第三方微信小程序客户端成功刷机的 **HCI 抓包**（逐帧还原）、老版官方 Flutter 应用 `DeepFanController`（Blutter 反编译）与真机实测。

---

## 1. 总览

| 版本 | 通信 | 协议形态 | 还原度 |
|---|---|---|---|
| 老版 W96D | USB：HID（Windows）/ bulk（安卓原生） | ASCII `命令=值,`（无帧头/校验） | ~95% |
| NEW W96D | BLE（Web Bluetooth） | 特征读写，控制值为 **ASCII hex 文本** | 100% |
| W96D Pro | BLE | 同 NEW W96D + 快充命令 | 100% |
| DFU/OTA（NEW W96D / Pro） | BLE `FEE0` | 二进制 `0x55` 帧 + CRC8 | 100%（真机刷机成功） |

三个版本本质是同一套「命令 = ASCII」思路，差别只在传输层与（新版的）hex 文本编码。

---

## 2. 老版 W96D（USB / HID）

### 2.1 硬件与通道

| 项 | 值 |
|---|---|
| USB ID | `0x0716:0x5103`（名称 **DeepFan**，实测批次）/ `0x0E2C:0xA206`（官方安卓 APK 内置） |
| 端点 | HID 类，无 reportId，64B 中断 IN#2 / OUT#1 |
| Windows / 桌面 | `hidapi`，浏览器侧唯一通道是 **WebHID** |
| 安卓 | 官方 APK 用原生 `UsbManager` + bulk 端点；浏览器 **WebUSB 无法声明 HID 类**（受保护类，全平台硬限制），Chrome 157 起计划支持安卓 WebHID，当前可用 Canary/Dev 的 `web-hid-on-android` flag |

### 2.2 下行命令（ASCII，`命令=值,`）

| 命令 | 含义 |
|---|---|
| `GEAR=<n>,` | 档位：`0` 停止，`1~4` 第 1~4 档（值直通） |
| `GCLR,` | 清除档位 PWM 设置 |
| `GSET1..4=<pwm>,` | 各档 PWM（1-100） |
| `WIND_SUM=<组数×10>,` | 自然风总点数（设备固定 10 点/组，组数 1-20） |
| `WIND_TIM=<秒>,` | 整条曲线一圈的总时长（1-3600） |
| `WIND_IDX=<序号×256+风量>,` | 逐点写入风量（0-100） |
| `WIND_SAVE=0,` | 保存曲线 |
| `WIND_CLR=0,` | 恢复默认曲线 |

### 2.3 上行遥测

约 100ms 一行逗号分隔 ASCII（行首带逗号）：

```
,SN=XXXXXX,Gear=0,Speed=0,VBus=5.0,VBat=4.2,IBat=0.00
```

字段：SN / 当前档位 / 转速 / 输入电压 V / 电池电压 V / 电池电流 A（充电为正）。

### 2.4 自然风曲线

10 点/组、**上限 20 组（200 点）**；`Time Cycle` 是**整条曲线一圈**的总时长（不是单组/单点）。上传顺序：
`WIND_SUM=<组数×10>,` → `WIND_TIM=<总秒数>,` → 逐条 `WIND_IDX=<序号×256+值>,` → `WIND_SAVE=0,`。

---

## 3. NEW W96D / W96D Pro（BLE）

### 3.1 通用约定

- 广播名前缀 `W96D`（工具按此过滤；Pro 为 `W96P`，名称后缀是 SN，如 `W96D_XXXXXX`）。
- 控制命令写成 **ASCII hex 文本**再编码写入（如转速 50% 写字符串 `"32"`），**多字节大端**。
- 转速 0-100%；定时 1-480 分钟。

### 3.2 GATT 总表

Base UUID `0000xxxx-0000-1000-8000-00805f9b34fb`。

**FFF0（主控）**

| 特征 | UUID | 读 | 写 |
|---|---|---|---|
| POWER | FFF1 | u8 | `00` 关 / `01` 开（写前自动关 Turbo/自然风） |
| TIMER | FFF2 | u16BE 剩余秒 | hex 文本 4 位（秒 = 分钟×60）；`0000` 取消 |
| FAN_SPEED | FFF3 | u8 当前 % | hex 文本 2 位 |
| NATURE_WIND | FFF4 | u8 0/1 | `01`/`00`（与 Turbo 互斥） |
| SHUTDOWN_DELAY | FFF5 | u16BE 秒 | hex 4 位；0=永不；1-9 自动修正为 10 |
| GEAR_DOWN_MODE | FFF6 | u8 0/1 | `00` 逐级减档 / `01` 直接回 0 |
| SPEED_CALIB | FFF7 | 4×u8 | hex 8 位（每档 0-100） |
| TURBO_TIME | FFF8 | u16BE 秒 | hex 4 位；0=默认 199 |
| LIGHT_CONTROL | FFFA | u8 | `00` 一键关灯 / `01` 恢复 |
| TURBO_REMAINING | FFFB | u16BE 秒 | — |
| TURBO_MODE | FFFC | u8 | `01`/`00` |

**FFD0（电源/电量，Pro 复用为快充，见 §4）**

| 特征 | UUID | 读 | 写 |
|---|---|---|---|
| BATTERY_INFO | FFD1 | 见 3.3 | ASCII `BAT_CAP=<mWh>,` |
| POWER_STATUS | FFD2 | 见 3.3 | — |
| MOTOR_INFO | FFD3 | 见 3.3 | — |
| POWER_CONFIG | FFD4 | — | （Pro 快充配置） |

**FFC0（SN 显示开关）**：`FFC1` 写 `BLE_SN=1,` / `BLE_SN=0,`（读回是 `READY,`，实际状态以设备名后缀为准）。

**FFE0（自然风曲线）**：`FFE3` 读写 **128 字节原始值**（每字节 0-100），设备循环播放；默认曲线见工具内常量。

**FEE0（DFU / 设备信息）**：写 `FEE1`、通知 `FEE2`，见 §5。

### 3.3 读布局

**BATTERY_INFO (FFD1)**：`0..1` u16BE 电池电压 mV｜`2..3` i16BE 电池电流 mA（正=放电）｜`4..7` u32BE 容量 mWh。

**POWER_STATUS (FFD2)**：`0..3` u32BE VBUS 电压 mV｜`4..5` i16BE VBUS 电流 mA（`0x7FFF` 视为 0）｜`7` u8 充放状态（1=充电中）。

**MOTOR_INFO (FFD3)**：`0..1` u16BE 电机电流 mA｜`2` u8 堵转标志｜末 2 字节 u16BE 电机电压 mV。

功率 W = 电压(mV) × 电流(mA) / 1e6。

---

## 4. W96D Pro 快充（FFD0）

| 特征 | 用途 |
|---|---|
| FFD1 (BATTERY_INFO) | 电压 u16BE @0 / 电流 i16BE @2 / 容量 u32BE @4 |
| FFD2 (POWER_Q1) | 读实时状态 + 写 `POW_C_IN=<0\|1>,`（C 口输入快充开关） |
| FFD4 (POWER_Q2) | 读配置 + 写 `POW_1A=` / `POW_1C=` / `POW_1D=` |

**POWER_Q1 (FFD2) 读**：`0..3` u32BE VBUS mV｜`4..5` i16BE 电流 mA（`0x7FFF`→0）｜`6` powC（输出使能）｜`7` powSta｜`8` powCOut｜`9` powCIn｜`10` powCHi。

**POWER_Q2 (FFD4) 读**：`1` powVer｜`2` powSink｜`3` powSrc｜`6` pow1a｜`7` pow1c｜`8` pow1d｜`9` pow1e｜`13` pow2a｜`14` pow2b｜`15` pow2c。

**写命令**（ASCII，实际发送 `命令 + ","`）：

| 命令 | 目标 | 位域（0=使能） | 默认 |
|---|---|---|---|
| `POW_C_IN=<v>,` | FFD2 | C 口输入快充开关 | `0` |
| `POW_1A=<hex>,` | FFD4 | bit2：12V 输入支持 | `0x1C` |
| `POW_1C=<hex>,` | FFD4 | bit4：PD Sink 使能 | `0x00` |
| `POW_1D=<hex>,` | FFD4 | bit7 FCP / bit3 AFC / bit1 SCP | `0x00` |

写后回读验证，命令节流 300ms，失败重试 3 次。⚠ 快充魔改官方声明高风险（可能烧设备）。

---

## 5. DFU / OTA 协议（FEE0）

### 5.1 帧格式与 CRC

```
偏移 0     : 0x55 帧头
偏移 1     : key（主机固定 0；见下方约束）
偏移 2..3  : payload 长度（LE u16）
偏移 4..   : payload
最后一字节 : CRC8
```

- **CRC8**：多项式 `0x3B`、**非反射（MSB-first）**、初值 `0x89`，覆盖 `frame[0 .. len+4)`。
- **设备→主机**：字节（≥2）与 `CRC8_TABLE[key]` 异或，key 每帧随机；CRC 对**解掩码后**的帧计算。
- ⚠ **CRC 表有两处易错值**：官方网页 `W96D_V3.8.html` 的表把下标 **120/202 写成 `200`**（真值 `102`/`101`）。短帧运算不会命中这两项 ⇒ 控制命令看起来一直正常；**长帧（102B 解锁头、134B 数据帧）CRC 会算错，设备静默丢弃**。本仓库 `tools/crc8_table.js` 用真值（依据：官方小程序抓包 673/673 条帧 CRC 通过）。

### 5.2 命令表（实测应答）

| 发送 payload | 应答 | 含义 |
|---|---|---|
| `0F` / `8F` GET_SN | `0a` + SN LE32 | 序列号（= 设备名后缀，如 `W96D_XXXXXX` → `0a 7b 00 00 00`） |
| `8A` GET_VERSION | `04` + 版本字节 | 版本（`0d` = V1.3，十进制拆 major/minor） |
| `85` CHECK_IN_DFU | `01` 应用态 / `02` 升级模式 | 阶段查询（`01` 不是错误） |
| `84` ENTER_DFU | `02` | 进入升级流程的前置命令 |
| `8F`「切换」 | **无应答** | 必须在 `84` 之后、**同一条连接上立刻**发；设备随即停 BLE |
| `8C` GET_PAGE_SIZE | `05` + u16LE | 页大小 `0x1000` = 4096 ⇒ 写块 128B |
| `81` + 96B 文件头 | `02` | 解锁 / 下发文件头（裸 `81` 回 `01`，无意义） |
| `87` START_UP | `02`（约 1.3s） | 擦除并开始接收数据 |
| `02` + 数据 | 无逐块回执 | 写 flash，地址由设备端自增 |
| `88` END_UP | `02`（设备收尾，可能十几秒到几十秒） | 结束传输 |
| `0B` RESET | 无应答 | 复位回应用模式 |

### 5.3 升级时序（官方客户端抓包实证）

```
0x0F/0x8F(GET_SN) → 0x8A(GET_VERSION) → 0x85(→01)
→ 0x84(→02) → 0x8F「切换」（同连接立刻发，无应答）
→ 设备停 BLE，链路约 5s 监督超时断开（reason 0x08）
→ 重连（同一地址，无需重扫；MTU 由 243 变 247）→ 重新订阅 FEE2
→ 0x85(→02) → 0x8C(→05 00 10)
→ 0x81 + 头 96B（整帧 102B → 02）→ 0x87(→02)
→ 0x02 + 128B × 638 + 末帧 100B（共 81764B）→ 0x88(→02) → 0x0B 复位
```

### 5.4 实现必须遵守的约束（易错点）

1. **key 必须为 0**：非 0 key 时连 7B 小命令都被静默（W66D APP 的动态 key 语义不适用于 W96D）。
2. **`0x8F` 的时机**：必须紧跟 `0x84` 的应答、在同一条连接上发出（抓包实测相隔 59ms）；**等断链重连后再发就无效**，设备只当普通 SN 查询。
3. **切换后必须重连并重新订阅 FEE2**，旧 characteristic 对象不可复用；`0x85` 回 `02` 才算进了升级模式。
4. **大帧单包发**：解锁头 102B、数据帧 134B 都是**一条 ATT 写**（无响应写）；传输阶段 MTU 247 足够。
5. **全部用无响应写（WRITE_CMD）**；`0x81`/`0x87`/`0x88` 的应答是 `02`（不是 `01`）。
6. **设备广播全程不变**（`|DFU` 标记在应用态也存在）⇒ 不能用广播判断"已进升级模式"，判据只有 `0x85` 的 `01/02`。

### 5.5 实测参数

| 项 | 应用态 | 升级模式 |
|---|---|---|
| MTU（手机请求 247） | 应答 243 | 应答 247 |
| 大帧 | 约 20B 以上静默 | 102B / 134B 均正常 |
| 数据阶段 | — | 639 帧（638×128B + 100B）= 81764B，间隔中位 18ms，约 55 帧/秒，全程约 12~15s |

### 5.6 固件包格式

**`.up`（BLE OTA）**：头部 96 字节明文，ASCII 字段以 `00` 分隔。

| 偏移 | 长度 | V1.3 实测 | 含义 |
|---|---|---|---|
| 0x00 | 32 | `105a109580404617889a6d116bbe7896` | 32 字符 hex 校验串（≠文件/体 MD5、CRC32，算法未定） |
| 0x21 | 7 | `W96D001` | 型号（工具据此校验适配性） |
| 0x32 | 8 | `00013F64` | **固件体长度**（hex）= 81764 = 总长 − 96 |
| 0x3B | 16 | `845EC`/`33E`/`000133A8` | 附加 hex 字段（疑似加密参数/入口，未定） |
| 0x4D | 1 | `0D`（V1.3） | **版本字节**，与 `0x8A` 编码一致 |

固件体自 `0x60` 起，**非明文 ARM 镜像**（无向量表特征，高频字节 0x43）⇒ 直传不解包。

| 文件 | 大小 | 格式 | 型号 | 版本 | 适用 |
|---|---|---|---|---|---|
| `W96D_V13.up` | 81860B | .up | `W96D001` | V1.3 | NEW W96D |
| `W96P_V1.8.up` | 117696B | .up | `W96P` | V1.8 | W96D Pro |
| `W96D_1.8.df` / `W96D_5.2.df` | 50240B | .df（**加密**） | 不可读 | 按文件名 | 老版 W96D，只能官方 `WITRN_UP` 烧录；两版唯一已知差异是机身「−」键行为 |

### 5.7 失败判读

| 现象 | 含义 | 处理 |
|---|---|---|
| `0x85` 回 `01` | 还在应用态，`0x8F` 切换没生效 | 确认 `0x8F` 是在 `0x84` 应答后同连接立刻发的 |
| 解锁帧无应答 | 帧内容/CRC 或状态不对 | 检查 CRC 表版本与 96B 头部 |
| `0x88` 首次无应答 | 设备正在写最后几页 + 校验 | 重试（可能需几十秒；设备自行重启也算完成） |
| 仍停在升级模式 | 写入未被接受（可能丢块） | 放慢写入间隔到 30~40ms 后重刷 |
| 版本号没变 | 同版本重刷（官方不拒绝同版本） | 正常，看设备功能 |

---

## 6. 工具实现（`W96D统一控制台.html`）

### 6.1 结构

单文件、零依赖，同一页面内三种传输：

```
传输层：bleConnect / usbConnect / hidConnect   → write + notify/读
协议层：BLE（特征表 + ASCII hex）、USB/HID（ASCII 命令）、DFU（0x55 帧）
界面层：控制页 / 自然风曲线 / 设备信息（含固件升级）/ 帮助
```

### 6.2 要点

- 连接走**页面内**的 `requestDevice`/`requestPort`，不使用系统配对；BLE 连接后自动读版本/SN/遥控状态。
- 统一 `logBuf` 记录所有连接方式的收发原始字节（可开关遥测日志）。
- 老版自然风与新版曲线是两套机制：老版走 `WIND_*` 序列，新版走 `FFE3` 128 字节直写。

### 6.3 OTA 实现

| 函数 | 职责 |
|---|---|
| `dfuPack` / `onDfuReceive` | 组帧/解帧（含掩码与 CRC 校验；结构完整但 CRC 不符时宽容采用并留痕） |
| `dfuRequest` | 写帧 + 等指定应答（丢弃错位应答；超时打印残留半帧） |
| `dfuWaitReconnect` | 等断链 → 重连 → 重订阅 → `0x85` 校验（结果记 `S.ble.lastCheckIn`） |
| `otaParseFile` | 解析 `.up` 头（型号/版本/体长） |
| `otaEnterTransferStage` | `0x85` → `0x84` → **同连接立刻 `0x8F`** → 断链重连 → 确认 `0x85→02` |
| `otaRun` | 完整流程：解锁头 → `0x87` → 逐块写入（可调间隔）→ `0x88`（3×20s 重试）→ `0x0B` → 回读版本 |

参数：`OTA_CHUNK=128`、写入间隔默认 20ms（界面可调）、收尾 `OTA_END_RETRIES=3` × `OTA_END_TIMEOUT=20s`（可被 `globalThis` 覆盖，供测试注入小值）。

### 6.4 安全边界

- `0x87` 之前**不发任何会写 flash 的命令**（解锁头只校验）。
- `0x87` 之后应用区已擦除，失败会明确提示"是否需要重刷"；设备停在升级模式时重新连接即可重刷。
- 型号不匹配（头部 `[0x21]` ≠ `W96D001`）时弹窗二次确认。

---

## 7. 证据与复现

| 内容 | 位置 |
|---|---|
| 抓包**关键数据**（链路事件、命令帧、设备通知、数据阶段、CRC 验收） | 本文 **附录 A** |
| 关键帧**字节快照**（7KB 文本，回归测试直接用） | `tools/tests/fixtures/key_frames.js` |
| 原始抓包（btsnoop，4940 包 / 106.3s / 354KB） | **已删除**（为精简仓库），如需重抓：安卓开发者选项开「蓝牙 HCI 信息收集日志」→ 关开蓝牙 → 用官方客户端完整刷一次 → 关开蓝牙落盘 `.cfa` |
| 抓包复盘工具（链路事件、命令→应答时序、数据阶段统计） | `node tools/parse_hci_log.js <抓包文件>` |
| CRC8 表（已修正 120/202） | `tools/crc8_table.js` |
| 回归测试（CRC 表验收、黄金帧比对、OTA 全流程 mock…） | `node tools/tests/run_all.js` |

黄金帧测试把抓包里官方客户端真实发出的帧（102B 解锁头、106B 尾帧、跨越固件体的 8 条 134B 数据帧）与工具 `dfuPack()` **逐字节比对**，是"帧布局 + CRC"最硬的验收；CRC 表两处修正即靠它发现并验证。解析器另有自建合成 btsnoop 的用例，因此**原始抓包不再随仓库保存**也不影响回归。

---

## 8. 外部文件与参考

| 文件 | 来源 | 用途 |
|---|---|---|
| `W66D_APP.zip` | <https://www.witrn.com/witrn/fan/W66D_APP.zip> | 官方 OTA/遥控 APK（`BleConstants`：服务 `FEE0`、写 `FFE1`、通知 `FFE2`、MTU 200、写块 197B、帧上限 300B） |
| `WITRN_UP_V30.zip` | WITRN 官网 | 老版 `.df` 固件官方烧录工具（.NET，协议族同源：`55` 帧 + HID） |
| `W96D_V3.8.html` | <https://www.witrn.com/witrn/fan/W96D_V3.8.html> | 官方蓝牙控制页（CRC8 表即出自它，含前述两处错值） |
| `W96P_charger.html` | WITRN 官网 | Pro 快充协议参考 |
| `W96DN_V1.3.zip` | <https://www.witrn.com/witrn/fan/W96DN_V1.3.zip> | 官网固件包（= `rom/W96D_V13.up`） |

相关开源项目：[Halcao/witrn-fan-control](https://github.com/Halcao/witrn-fan-control)（唯一 W96D 开源项目，仅控制，独立印证 GATT 映射）、[openwch/ch592](https://github.com/openwch/ch592)（主控为沁恒 CH592F；官方 BLE OTA profile 同为 `0xFEE0/0xFEE1`，但**命令层是 WITRN 自研**，不能用 WCH 工具刷 W96D）、[sintrb/WCHOTA](https://github.com/sintrb/WCHOTA)、[luckyPtr/wch-ota-tool](https://github.com/luckyPtr/wch-ota-tool)。

> 本文档为逆向研究结果，与 WITRN 官方无关；固件版权归 WITRN。
>
> **隐私处理**：文档与测试夹具中的对端蓝牙地址已替换为占位符（`aa:bb:cc:dd:ee:ff`）、
> 设备序列号已隐去（示例写作 `W96D_XXXXXX` / `XXXXXX`），含真实 SN 的通知帧未收录；
> 抓包文件本身已从仓库删除。协议结构、帧字节（除 SN 载荷）、掩码与 CRC 规则均为原始数据。

---

## 附录 A：抓包关键数据（原文已删，以下为提取的核心证据）

原抓包（安卓手机蓝牙 HCI 日志，btsnoop，4940 包 / 106.3s / 354KB；文件名与设备地址、SN 均已隐去）是本文档全部结论的原始依据；
为精简仓库已删除，**关键帧的字节快照保留在 `tools/tests/fixtures/key_frames.js`**（7KB 文本，回归测试直接使用）。

### A.1 连接与链路事件

| 时刻 | 事件 |
|---|---|
| +21946ms | 连接建立｜handle=0x201 对端=aa:bb:cc:dd:ee:ff |
| +22485ms | 连接参数更新｜间隔=7.5ms 监督超时=5000ms |
| +23407ms | 连接参数更新｜间隔=48.75ms 监督超时=5000ms |
| +24337ms | 连接参数更新｜间隔=7.5ms 监督超时=5000ms |
| +25214ms | 连接参数更新｜间隔=48.75ms 监督超时=5000ms |
| +25854ms | 连接参数更新｜间隔=7.5ms 监督超时=5000ms |
| +25945ms | 连接参数更新｜间隔=48.75ms 监督超时=5000ms |
| +26576ms | 断开｜handle=0x201 原因=0x16 |
| +28431ms | 连接建立｜handle=0x201 对端=aa:bb:cc:dd:ee:ff |
| +29017ms | 连接参数更新｜间隔=7.5ms 监督超时=5000ms |
| +29819ms | MTU 请求｜247 |
| +29835ms | MTU 应答｜243 |
| +30135ms | 连接参数更新｜间隔=48.75ms 监督超时=5000ms |
| +52517ms | 断开｜handle=0x201 原因=0x8（监督超时→设备重启） |
| +63123ms | 连接建立｜handle=0x201 对端=aa:bb:cc:dd:ee:ff |
| +63757ms | 连接参数更新｜间隔=7.5ms 监督超时=5000ms |
| +64546ms | MTU 请求｜247 |
| +64560ms | MTU 应答｜243 |
| +64602ms | 连接参数更新｜间隔=48.75ms 监督超时=5000ms |
| +67780ms | 连接参数更新｜间隔=7.5ms 监督超时=5000ms |
| +67869ms | 连接参数更新｜间隔=48.75ms 监督超时=5000ms |
| +67917ms | 断开｜handle=0x201 原因=0x16 |
| +68749ms | 连接建立｜handle=0x201 对端=aa:bb:cc:dd:ee:ff |
| +69338ms | 连接参数更新｜间隔=7.5ms 监督超时=5000ms |
| +70115ms | MTU 请求｜247 |
| +70159ms | 连接参数更新｜间隔=48.75ms 监督超时=5000ms |
| +70207ms | MTU 应答｜243 |
| +81475ms | 断开｜handle=0x201 原因=0x8（监督超时→设备重启） |
| +82035ms | 连接建立｜handle=0x201 对端=aa:bb:cc:dd:ee:ff |
| +82575ms | 连接参数更新｜间隔=7.5ms 监督超时=5000ms |
| +82837ms | 连接参数更新｜间隔=48.75ms 监督超时=5000ms |
| +83285ms | MTU 请求｜247 |
| +83370ms | MTU 应答｜247 |
| +103459ms | 断开｜handle=0x201 原因=0x8（监督超时→设备重启） |
| +105813ms | 断开｜handle=0x33 原因=0x16 |

### A.2 主机写的命令帧（原始字节；帧尾即 CRC8）

| 时刻 | 帧 | 说明 |
|---|---|---|
| +30796ms | `55 00 01 00 0f b7` | GET_SN |
| +31081ms | `55 00 01 00 8a 29` | GET_VERSION |
| +66091ms | `55 00 01 00 0f b7` | GET_SN |
| +66390ms | `55 00 01 00 8a 29` | GET_VERSION |
| +71694ms | `55 00 01 00 0f b7` | GET_SN |
| +71976ms | `55 00 01 00 8a 29` | GET_VERSION |
| +75763ms | `55 00 01 00 8f fe` | GET_SN / 「切换」 |
| +75921ms | `55 00 01 00 8f fe` | GET_SN / 「切换」 |
| +76067ms | `55 00 01 00 8a 29` | GET_VERSION |
| +76213ms | `55 00 01 00 85 6b` | CHECK_IN_DFU |
| +76360ms | `55 00 01 00 84 50` | ENTER_DFU |
| +76506ms | `55 00 01 00 8f fe` | GET_SN / 「切换」 |
| +83737ms | `55 00 01 00 85 6b` | CHECK_IN_DFU |
| +83823ms | `55 00 01 00 8c b3` | GET_PAGE_SIZE |
| +84917ms | `55 00 01 00 87 1d` | START_UP |
| +98254ms | `55 00 01 00 88 5f` | END_UP |
| +98418ms | `55 00 01 00 0b 5b` | RESET |
| +83920ms | `55 00 61 00 81 31 30 35 61 31 30 39 35 38 30 34 30 34 36 31 …`（全长 102B） | REQ_UNLOCK + 96B 文件头 |
| +98233ms | `55 00 65 00 02 c4 43 c4 43 26 43 c4 43 84 43 c4 43 18 43 c4 …`（全长 106B） | 末帧数据（100B） |

### A.3 设备通知（原始线上字节 + 按 `CRC8_TABLE[key]` 解掩码后的载荷）

| 时刻 | key | 原始字节 | 解掩码载荷 | 含义 |
|---|---|---|---|---|
| +31159ms | 0x43 | `55 43 f6 f4 f0 f9 0a` | `04 0d` | `040d` = 版本 V1.3 |
| +66455ms | 0x5b | `55 5b e8 ea ee e7 12` | `04 0d` | `040d` = 版本 V1.3 |
| +72060ms | 0x42 | `55 42 cd cf cb c2 e2` | `04 0d` | `040d` = 版本 V1.3 |
| +76155ms | 0x41 | `55 41 80 82 86 8f e1` | `04 0d` | `040d` = 版本 V1.3 |
| +76301ms | 0xd4 | `55 d4 e0 e1 e0 4c` | `01` | `01` = 应用态 |
| +76447ms | 0x7f | `55 7f c6 c7 c5 83` | `02` | `02` = 升级模式 / OK |
| +83809ms | 0x25 | `55 25 17 16 14 40` | `02` | `02` = 升级模式 / OK |
| +83907ms | 0x87 | `55 87 eb e8 ed e8 f8 66` | `05 00 10` | `050010` = 页大小 0x1000 |
| +84004ms | 0xec | `55 ec 3f 3e 3c bc` | `02` | `02` = 升级模式 / OK |
| +86247ms | 0x89 | `55 89 90 91 93 bc` | `02` | `02` = 升级模式 / OK |
| +98387ms | 0x12 | `55 12 8a 8b 89 bc` | `02` | `02` = 升级模式 / OK |

> 设备 SN 应答帧（5 条）与对端蓝牙地址已按隐私要求隐去；SN 应答的结构为 `0a + SN LE32`，
> 掩码与 CRC 规则与上表其它帧完全相同。

### A.4 数据阶段

- 数据帧 **638 条 134B**（128B/帧）+ 1 条 106B 尾帧 = **81764B**（= 81860 − 96）
- 帧间隔 min/中位/max = 13/18/24ms，约 54.8 帧/秒，全程 11.7s；**无逐块回执**
- 抽样帧（块号 0, 50, 150, 250, 350, 450, 550, 637）已存夹具，与工具 dfuPack 输出逐字节一致

### A.5 CRC8 表验收（这笔证据直接定位了「长帧静默」的真因）

| 帧类型 | 官方网页表的表值 | poly=0x3B 真值表 |
|---|---|---|
| 6B 控制帧（17 条） | 17/17 | 17/17（两表在这些输入上恰好同值） |
| 102B 解锁头 | **0/1** | 1/1 |
| 106B 尾帧 | **0/1** | 1/1 |
| 134B 数据帧（638 条） | **251/638** | **638/638** |
| 设备通知（16 条，解掩码后） | 16/16 | 16/16 |

⇒ 官方网页表把下标 **120/202 写成 200**（真值 102/101）；短帧运算不会命中这两项，所以控制命令一直正常，
**一旦载荷变长就必然踩到，CRC 算错、设备静默丢弃**。`tools/crc8_table.js` 用真值。
